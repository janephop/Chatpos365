# 🚀 เริ่ม Backend และ Frontend

## ❌ ปัญหา

URLs ไม่ทำงานเพราะ:
- ❌ Backend (port 3000) ยังไม่ได้รัน
- ❌ Frontend (port 8085) ยังไม่ได้รัน

---

## ✅ วิธีแก้ไข: เริ่ม Backend และ Frontend

### วิธีที่ 1: ใช้สคริปต์ (แต่แก้ไขเล็กน้อย)

**⚠️ หมายเหตุ:** `START_PERMANENT.bat` จะรัน tunnel แบบ manual แต่เราใช้ Managed Service แล้ว

**แก้ไข:** ใช้สคริปต์แค่เริ่ม Backend และ Frontend

**สร้างสคริปต์ใหม่ `START_SERVERS_ONLY.bat`:**

```batch
@echo off
chcp 65001 >nul
echo ========================================
echo  🚀 เริ่มต้น Backend และ Frontend
echo ========================================
echo.

echo 🔵 เริ่มต้น Backend Server (port 3000)...
start "Backend Server (3000)" cmd /k "cd line-webhook && npm start"

timeout /t 3 /nobreak >nul

echo.
echo 🔵 เริ่มต้น Frontend Server (port 8085)...
start "Frontend Server (8085)" cmd /k "npm run dev"

timeout /t 3 /nobreak >nul

echo.
echo ========================================
echo  ✅ เริ่มต้น Backend และ Frontend แล้ว!
echo ========================================
echo.
echo 💡 Tunnel ทำงานผ่าน Managed Service แล้ว
echo    ไม่ต้องรัน tunnel แบบ manual
echo.
pause
```

---

### วิธีที่ 2: รันเอง (แนะนำ)

**Terminal 1 - Backend:**
```bash
cd line-webhook
npm start
```

**Terminal 2 - Frontend:**
```bash
npm run dev
```

---

## ✅ หลังจากเริ่ม Backend และ Frontend แล้ว

**ตรวจสอบ:**
1. **Backend ทำงาน:**
   - เปิดเบราว์เซอร์: `http://localhost:3000`
   - ควรเห็น response จาก Backend

2. **Frontend ทำงาน:**
   - เปิดเบราว์เซอร์: `http://localhost:8085`
   - ควรเห็นหน้า Frontend

3. **ทดสอบ URLs:**
   - Backend: `https://line-chat-backend-12345.trycloudflare.com`
   - Frontend: `https://line-chat-frontend-12345.trycloudflare.com`

---

## 💡 หมายเหตุ

**สำหรับ Managed Service:**
- ✅ Service ทำงานอยู่แล้ว (ไม่ต้องรัน tunnel แบบ manual)
- ✅ Routes ถูกตั้งค่าแล้ว
- ✅ แค่เริ่ม Backend และ Frontend ก็พอ

**ไม่ต้องรัน:**
- ❌ `cloudflared.exe tunnel run` (ใช้ Service แทน)

---

<div align="center">
✅ **เริ่ม Backend และ Frontend ก่อน แล้วทดสอบ URLs อีกครั้ง!** 🚀
</div>

