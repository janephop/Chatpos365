# ⚡ ใช้ Quick Tunnel ชั่วคราว (แก้ปัญหา Domain not resolved)

## ❌ ปัญหา

Error: "A domain name that can't be resolved or an incorrect parameter or value may have been specified"

**สาเหตุ:**
- Hostname ใน config file ยังไม่ resolve
- Routes ยังไม่ได้ sync กับ DNS

---

## ✅ วิธีแก้ไข: ใช้ Quick Tunnel ชั่วคราว

**Quick Tunnel จะสร้าง URL ให้อัตโนมัติ (ไม่ต้องใช้ config file)**

---

## 🚀 ขั้นตอน

### ขั้นตอนที่ 1: รัน Quick Tunnel สำหรับ Backend

**Terminal ใหม่:**

```powershell
cd "C:\Users\jay_rpn\Documents\Project Big\Project Pos Chat\line chat"
cloudflared.exe tunnel --url http://localhost:3000
```

**หรือถ้าไม่พบ cloudflared.exe:**
```powershell
cloudflared tunnel --url http://localhost:3000
```

**⚠️ ปล่อย Terminal นี้ไว้ (ไม่ต้องปิด)**

**สิ่งที่ควรเห็น:**
```
2025-12-20T... INF +--------------------------------------------------------------------------------------------+
2025-12-20T... INF |  Your quick Tunnel has been created! Visit it at (it may take some time to be reachable): |
2025-12-20T... INF |  https://xxxx-xxxx-xxxx.trycloudflare.com                                                 |
2025-12-20T... INF +--------------------------------------------------------------------------------------------+
```

**จด URL ที่ได้ไว้** (เช่น `https://xxxx-xxxx-xxxx.trycloudflare.com`)

---

### ขั้นตอนที่ 2: รัน Quick Tunnel สำหรับ Frontend

**Terminal ใหม่:**

```powershell
cd "C:\Users\jay_rpn\Documents\Project Big\Project Pos Chat\line chat"
cloudflared.exe tunnel --url http://localhost:8085
```

**⚠️ ปล่อย Terminal นี้ไว้ (ไม่ต้องปิด)**

**จด URL ที่ได้ไว้** (เช่น `https://yyyy-yyyy-yyyy.trycloudflare.com`)

---

### ขั้นตอนที่ 3: ทดสอบ URLs

**หลังจากรัน Quick Tunnel แล้ว:**

1. **รอสักครู่** (10-30 วินาที)

2. **ทดสอบ Backend:**
   - เปิดเบราว์เซอร์: URL ที่ได้จาก Terminal (เช่น `https://xxxx-xxxx-xxxx.trycloudflare.com`)
   - ควรเห็น response จาก Backend

3. **ทดสอบ Frontend:**
   - เปิดเบราว์เซอร์: URL ที่ได้จาก Terminal (เช่น `https://yyyy-yyyy-yyyy.trycloudflare.com`)
   - ควรเห็นหน้า Frontend

---

### ขั้นตอนที่ 4: ตั้งค่า LINE Webhook

1. **ไปที่ LINE Developers Console:**
   - URL: https://developers.line.biz/console/

2. **เลือก Provider** → **Channel** → **Messaging API** tab

3. **Webhook settings** → **Edit**

4. **ใส่ Webhook URL:**
   ```
   https://xxxx-xxxx-xxxx.trycloudflare.com/webhook/line
   ```
   (ใช้ URL ที่ได้จาก Backend Quick Tunnel)

5. **เปิด "Use webhook"** = **Enabled**

6. **คลิก "Verify"** เพื่อทดสอบ

7. **เสร็จแล้ว!** ✅

---

## ⚠️ หมายเหตุ

**สำหรับ Quick Tunnel:**
- ✅ ใช้งานได้ทันที
- ✅ ไม่ต้องตั้งค่า config file
- ❌ URL จะเปลี่ยนทุกครั้งที่รันใหม่
- ❌ ต้องรัน Terminal ไว้ (ไม่ต้องปิด)

**ถ้าปิด Terminal:**
- Tunnel จะหยุดทำงาน
- URLs จะไม่ทำงาน

---

## 💡 วิธีแก้ไขถาวร (ภายหลัง)

**ถ้าต้องการ URL คงที่:**
1. รอให้ Routes ใน Dashboard sync กับ DNS
2. หรือใช้ Named Tunnel แบบ manual (หลังจาก DNS resolve แล้ว)

---

<div align="center">
✅ **ใช้ Quick Tunnel ชั่วคราวเพื่อแก้ปัญหา Domain not resolved!** 🚀

💡 **URLs จะเปลี่ยนทุกครั้ง แต่ใช้งานได้ทันที!**
</div>

