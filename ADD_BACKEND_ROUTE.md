# ✅ เพิ่ม Backend Route

## ✅ คุณอยู่ที่หน้าถูกต้องแล้ว!

**หน้า "Hostname routes"** - ถูกต้องแล้ว! ✅

---

## 📋 สถานะปัจจุบัน

**มี Routes แล้ว:**
- ✅ Frontend: `line-chat-frontend-12345.trycloudflare.com` ✅

**ยังไม่มี:**
- ⚠️ Backend: ยังไม่มี (ต้องเพิ่ม)

---

## 🎯 ขั้นตอนเพิ่ม Backend Route

### ขั้นตอนที่ 1: คลิกปุ่ม "+ Add hostname route"

**ในหน้า "Your hostname routes":**

1. **มองหาปุ่มสีน้ำเงิน** ด้านซ้าย: **"+ Add hostname route"**
2. **คลิกปุ่มนี้**

---

### ขั้นตอนที่ 2: กรอกฟอร์ม Backend

**หลังจากคลิกปุ่มแล้ว จะเห็นฟอร์ม:**

1. **Hostname (Required):**
   ```
   line-chat-backend-12345.trycloudflare.com
   ```
   - **สำคัญ:** ต้องมี `.trycloudflare.com` ครบถ้วน
   - ใช้ตัวเลขเดียวกับ Frontend (`12345`)

2. **Description:**
   ```
   Backend API
   ```
   - หรือ `website hostname` ก็ได้

3. **คลิก "Create"**

**หมายเหตุ:** Service URL (`http://localhost:3000`) จะถูกใช้จากไฟล์ config อัตโนมัติ

---

## ✅ หลังจากสร้าง Backend สำเร็จ

**คุณจะเห็นในตาราง 2 routes:**
- ✅ Backend: `line-chat-backend-12345.trycloudflare.com`
- ✅ Frontend: `line-chat-frontend-12345.trycloudflare.com`

**ตรวจสอบ:**
1. **กลับไปที่หน้า Connectors**
2. **ดูที่คอลัมน์ "Routes"** สำหรับ tunnel "line-chat"
   - ควรแสดงเป็น **`2`** แทน `--` ✅
   - หรืออาจยังเป็น `--` (Dashboard อาจต้องใช้เวลา sync)

---

## 🎉 เสร็จแล้ว!

**ตอนนี้มี Routes ครบแล้ว:**
- ✅ Backend URL: `https://line-chat-backend-12345.trycloudflare.com`
- ✅ Frontend URL: `https://line-chat-frontend-12345.trycloudflare.com`
- ✅ URLs คงที่ถาวร - ไม่ต้องเปลี่ยน webhook!

---

## 🚀 ขั้นตอนต่อไป

### 1. ตั้งค่า Frontend (.env)

**สร้างไฟล์ `.env` ในโฟลเดอร์ root:**

```
VITE_API_URL=https://line-chat-backend-12345.trycloudflare.com
```

### 2. เริ่ม Backend และ Frontend

```bash
START_PERMANENT.bat
```

### 3. ตั้งค่า LINE Webhook

- URL: `https://line-chat-backend-12345.trycloudflare.com/webhook/line`

---

<div align="center">
✅ **คลิกปุ่ม "+ Add hostname route" เพื่อเพิ่ม Backend!** 🚀

🎉 **หลังจากนั้นจะมี Routes ครบ 2 ตัวแล้ว!**
</div>

