# 🔧 แก้ปัญหา: Port 3000 ถูกใช้งานอยู่แล้ว

## ❌ ปัญหา

Error: `EADDRINUSE: address already in use :::3000`

**หมายความว่า:** Port 3000 ถูกใช้งานอยู่แล้ว (อาจเป็น Backend เก่าที่ยังทำงานอยู่)

---

## ✅ วิธีแก้ไข

### วิธีที่ 1: หยุด Process ที่ใช้ Port 3000

**ใน PowerShell:**

1. **หาว่า process ไหนใช้ port 3000:**
   ```powershell
   Get-NetTCPConnection -LocalPort 3000 | Select-Object OwningProcess
   ```

2. **หยุด process:**
   ```powershell
   Stop-Process -Id <PID> -Force
   ```
   (เปลี่ยน `<PID>` เป็นตัวเลขที่ได้จากขั้นตอนที่ 1)

---

### วิธีที่ 2: หยุด Process ทั้งหมดที่ชื่อ node

**ถ้า Backend เก่ายังทำงานอยู่:**

```powershell
Stop-Process -Name node -Force
```

**หรือหยุดเฉพาะ process ที่ใช้ port 3000:**

```powershell
$port = Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue
if ($port) {
    Stop-Process -Id $port.OwningProcess -Force
    Write-Host "✅ หยุด process ที่ใช้ port 3000 แล้ว"
} else {
    Write-Host "ℹ️ ไม่พบ process ที่ใช้ port 3000"
}
```

---

### วิธีที่ 3: ใช้ Task Manager

1. **กด Ctrl + Shift + Esc** (เปิด Task Manager)
2. **หาว่า process ไหนใช้ port 3000:**
   - ดูที่ "Node.js" หรือ "node"
3. **คลิกขวา** → **End Task**

---

## ✅ หลังจากหยุด Process แล้ว

**เริ่ม Backend ใหม่:**

```bash
cd line-webhook
npm start
```

**หรือใช้สคริปต์:**
```bash
START_SERVERS_ONLY.bat
```

---

## 🔍 ตรวจสอบว่า Port ว่างแล้ว

**ใน PowerShell:**
```powershell
Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue
```

**ถ้าไม่เห็นอะไร = Port ว่างแล้ว** ✅

---

<div align="center">
✅ **หยุด process ที่ใช้ port 3000 แล้วเริ่ม Backend ใหม่!** 🚀
</div>

