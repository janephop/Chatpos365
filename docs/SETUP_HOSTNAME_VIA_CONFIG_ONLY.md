# ⚡ ตั้งค่า Hostname ผ่านไฟล์ Config เท่านั้น (ไม่ต้องใช้ Dashboard!)

## ❓ ปัญหา: หา Public Hostnames ใน Dashboard ไม่เจอ

เมื่อพยายามหา Public Hostnames ใน Dashboard แต่:
- ไม่เห็นแท็บ Public Hostnames
- URL redirect ไป overview
- UI เปลี่ยนไปแล้ว

**วิธีแก้:** ใช้ไฟล์ Config เท่านั้น! (ไม่ต้องใช้ Dashboard)

---

## ✅ วิธีแก้ไข: ตั้งค่าผ่านไฟล์ Config เท่านั้น

### ขั้นตอนที่ 1: แก้ไขไฟล์ Config

1. **เปิดไฟล์** `cloudflare-tunnel-config.yml`

2. **แก้ไข hostname** ให้ตรงกับที่ต้องการ:

   ```yaml
   tunnel: ea4f26bd-8e88-4941-bc4d-2bf4a47e3abd
   credentials-file: C:\Users\jay_rpn\.cloudflared\ea4f26bd-8e88-4941-bc4d-2bf4a47e3abd.json

   ingress:
     # Backend (port 3000)
     - hostname: line-chat-backend-12345.trycloudflare.com
       service: http://localhost:3000
     # Frontend (port 8085)
     - hostname: line-chat-frontend-12345.trycloudflare.com
       service: http://localhost:8085
     # Catch-all (ต้องมีเสมอ)
     - service: http_status:404
   ```

   **เปลี่ยน `12345` เป็นตัวเลขที่ต้องการ** (เช่น: `12345`, `99999`, `2024`)

3. **บันทึกไฟล์** (Ctrl + S)

---

### ขั้นตอนที่ 2: ตั้งค่า Public Hostname ใน Dashboard (ผ่าน Migration)

**⚠️ สำคัญ:** ต้องทำ Migration **ครั้งเดียว** เพื่อให้ Cloudflare รู้จัก hostname

1. **ไปที่หน้า Migration:**
   ```
   https://one.dash.cloudflare.com/f0ba9c6582ac2ca4580daf1c665ecc55/networks/connectors/cloudflare-tunnels/ea4f26bd-8e88-4941-bc4d-2bf4a47e3abd/migrate
   ```

2. **คลิก**: **"Start migration"**

3. **ทำตาม Wizard:**
   - **Step 1:** คลิก "Confirm"
   - **Step 2:** ตรวจสอบ hostname ที่แสดง (จากไฟล์ config) แล้วคลิก "Next"
   - **Step 3:** คลิก "Next"
   - **Step 4:** คลิก "Finalize migration"

4. **เสร็จ!** Hostname จะถูกตั้งค่าอัตโนมัติจากไฟล์ config

---

### ขั้นตอนที่ 3: รัน Tunnel

**ใน PowerShell:**

```powershell
cloudflared.exe tunnel run line-chat --config cloudflare-tunnel-config.yml
```

**สิ่งที่เกิดขึ้น:**
- Tunnel จะรันและใช้ config จากไฟล์
- Hostname จะทำงานอัตโนมัติ
- **ไม่ต้องตั้งค่าเพิ่มเติมใน Dashboard!**

---

## 🔍 วิธีตรวจสอบว่า Hostname ทำงาน

### ตรวจสอบด้วยคำสั่ง

**ใน PowerShell:**

```powershell
cloudflared.exe tunnel route dns list line-chat
```

**จะเห็น hostname ที่ตั้งไว้**

### ตรวจสอบใน Dashboard

1. **ไปที่หน้า Connectors**
2. **คลิก tunnel "line-chat"**
3. **ดูที่ Routes** - ควรเห็น hostname ที่ตั้งไว้ (ถ้า Migration สำเร็จ)

---

## 📋 Checklist

- [ ] แก้ไขไฟล์ `cloudflare-tunnel-config.yml`
- [ ] เปลี่ยน hostname ให้ตรงกับที่ต้องการ
- [ ] บันทึกไฟล์
- [ ] ทำ Migration (ครั้งเดียว)
- [ ] รัน tunnel
- [ ] ทดสอบ URL

---

## 💡 หมายเหตุ

- **ไฟล์ Config** = ตั้งค่า hostname ในไฟล์ (หลัก)
- **Migration** = sync hostname จากไฟล์ไป Dashboard (ทำครั้งเดียว)
- **หลังจาก Migration** = ใช้ไฟล์ config ได้เลย ไม่ต้องตั้งค่าใน Dashboard อีก

---

## 🆘 ถ้ายังมีปัญหา

### ปัญหา: Migration ไม่สำเร็จ

**แก้ไข:**
1. ตรวจสอบว่า Tunnel ทำงานอยู่
2. ตรวจสอบไฟล์ config
3. ลอง Migration อีกครั้ง

### ปัญหา: Hostname ไม่ทำงาน

**แก้ไข:**
1. ตรวจสอบว่า Tunnel ยังรันอยู่
2. ตรวจสอบไฟล์ config
3. Restart tunnel

---

<div align="center">
✅ **วิธีง่าย: แก้ไขไฟล์ config แล้วทำ Migration ครั้งเดียว!**

🚀 **ไม่ต้องหา Public Hostnames ใน Dashboard อีก!**
</div>

