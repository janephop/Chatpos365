# ✅ ขั้นตอนต่อไปหลังจาก Login สำเร็จ

## 🎉 Login สำเร็จแล้ว!

ตอนนี้คุณได้ Login เข้า Cloudflare Tunnel สำเร็จแล้ว

---

## 📋 ขั้นตอนต่อไป (ทำตามลำดับ)

### ขั้นตอนที่ 1: สร้าง Tunnel

**ใน PowerShell หรือ Command Prompt:**

```cmd
cloudflared.exe tunnel create line-chat
```

**สิ่งที่เกิดขึ้น:**
- จะแสดง Tunnel ID (ตัวอักษรยาวๆ เช่น: `a1b2c3d4-e5f6-7890-abcd-ef1234567890`)
- **จด Tunnel ID ไว้** (หรือไม่จดก็ได้ เพราะเก็บไว้ในไฟล์แล้ว)

**✅ Tunnel สร้างสำเร็จ!**

---

### ขั้นตอนที่ 2: ตั้งค่า Public Hostname ใน Dashboard

#### 2.1 ไปที่ Cloudflare Dashboard

1. เปิดเบราว์เซอร์
2. ไปที่: **https://one.dash.cloudflare.com/**
3. **Login** เข้าระบบ (ถ้ายังไม่ได้ login)

#### 2.2 ไปที่หน้า Tunnels

1. **คลิกเมนูด้านซ้าย**: **"Zero Trust"**
2. **คลิก**: **"Networks"** (ในเมนูด้านซ้าย)
3. **คลิก**: **"Tunnels"** หรือ **"Connectors"** (ในเมนูด้านซ้าย)

#### 2.3 หา Tunnel "line-chat"

1. **มองหา** tunnel ชื่อ **"line-chat"** ในรายการ
2. **คลิก** ที่ tunnel "line-chat"

#### 2.4 ไปที่แท็บ Public Hostnames

**วิธีที่ 1: หาแท็บ (ปกติ)**
1. **มองหาที่ด้านบน** ของหน้า (ใต้ชื่อ tunnel "line-chat")
2. **คลิกแท็บ**: **"Public Hostnames"**

**วิธีที่ 2: ใช้ URL โดยตรง (ถ้าไม่เห็นแท็บ)**
1. **Copy URL** จาก address bar
2. **เพิ่ม** `/public-hostnames` ต่อท้าย URL
3. **Paste** ใน address bar แล้วกด Enter

#### 2.5 เพิ่ม Public Hostname สำหรับ Backend

1. **คลิกปุ่ม**: **"Add a public hostname"**

2. **กรอกฟอร์ม:**

   **Subdomain:**
   ```
   line-chat-backend-12345
   ```
   - เปลี่ยน `12345` เป็นตัวเลขที่ต้องการ

   **Domain:**
   ```
   trycloudflare.com  ← เลือกอันนี้! (ฟรี ไม่ต้องซื้อ)
   ```
   - **คลิก dropdown** → **เลือก**: **`trycloudflare.com`**

   **Service:**
   ```
   http://localhost:3000
   ```

3. **คลิก**: **"Add hostname"**

4. **จด URL ที่ได้ไว้:**
   - ตัวอย่าง: `https://line-chat-backend-12345.trycloudflare.com`

#### 2.6 เพิ่ม Public Hostname สำหรับ Frontend

1. **คลิกปุ่ม**: **"Add a public hostname"** อีกครั้ง

2. **กรอกฟอร์ม:**

   **Subdomain:**
   ```
   line-chat-frontend-12345
   ```
   - ใช้ตัวเลขเดียวกันกับ Backend

   **Domain:**
   ```
   trycloudflare.com  ← เลือกอันนี้!
   ```

   **Service:**
   ```
   http://localhost:8085
   ```

3. **คลิก**: **"Add hostname"**

4. **จด URL ที่ได้ไว้:**
   - ตัวอย่าง: `https://line-chat-frontend-12345.trycloudflare.com`

---

### ขั้นตอนที่ 3: แก้ไขไฟล์ Config

1. **เปิดไฟล์** `cloudflare-tunnel-config.yml` ด้วย Notepad

2. **แก้ไข hostname** ให้ตรงกับที่ตั้งใน Dashboard:

   ```yaml
   tunnel: <TUNNEL_ID>  # ไม่ต้องแก้ (ถ้ามีอยู่แล้ว)
   credentials-file: .cloudflared\<TUNNEL_ID>.json

   ingress:
     - hostname: line-chat-backend-12345.trycloudflare.com  # ← แก้ไขตรงนี้
       service: http://localhost:3000
     - hostname: line-chat-frontend-12345.trycloudflare.com  # ← แก้ไขตรงนี้
       service: http://localhost:8085
     - service: http_status:404
   ```

3. **บันทึก** (Ctrl + S)

---

### ขั้นตอนที่ 4: สร้างไฟล์ .env

#### 4.1 สร้างไฟล์ .env สำหรับ Frontend

1. **สร้างไฟล์** `.env` ในโฟลเดอร์ root
2. **พิมพ์:**
   ```env
   VITE_API_URL=https://line-chat-backend-12345.trycloudflare.com
   ```
   (เปลี่ยน `12345` ให้ตรงกับ Backend URL)

#### 4.2 สร้างไฟล์ .env สำหรับ Backend

1. **สร้างไฟล์** `line-webhook\.env`
2. **พิมพ์:**
   ```env
   LINE_CHANNEL_ACCESS_TOKEN=your_token_here
   LINE_CHANNEL_SECRET=your_secret_here
   PORT=3000
   ```

---

### ขั้นตอนที่ 5: เริ่มต้นระบบ

**ใช้สคริปต์อัตโนมัติ:**

```cmd
START_PERMANENT.bat
```

**หรือรันเอง:**

```cmd
# Terminal 1: Backend
cd line-webhook
npm start

# Terminal 2: Frontend
npm run dev

# Terminal 3: Tunnel
cloudflared.exe tunnel run line-chat --config cloudflare-tunnel-config.yml
```

---

### ขั้นตอนที่ 6: ตั้งค่า LINE Webhook

1. ไปที่: **https://developers.line.biz/console/**
2. เลือก Provider > Channel > **Messaging API** tab
3. **Webhook settings** > Edit
4. ใส่: `https://line-chat-backend-12345.trycloudflare.com/webhook/line`
5. เปิด **Use webhook** = Enabled
6. กด **Verify**

---

## ✅ Checklist

- [ ] สร้าง Tunnel: `cloudflared.exe tunnel create line-chat`
- [ ] ไปที่ Cloudflare Dashboard
- [ ] ตั้งค่า Public Hostname (Backend)
- [ ] ตั้งค่า Public Hostname (Frontend)
- [ ] แก้ไขไฟล์ `cloudflare-tunnel-config.yml`
- [ ] สร้างไฟล์ `.env` (Frontend)
- [ ] สร้างไฟล์ `line-webhook\.env` (Backend)
- [ ] เริ่มต้นระบบ
- [ ] ตั้งค่า LINE Webhook

---

## 📚 อ่านเพิ่มเติม

- [คู่มือตั้งค่าแบบละเอียด](SETUP_STEP_BY_STEP.md) - ทุกขั้นตอนแบบละเอียด
- [แก้ปัญหาไม่เห็น Public Hostnames](CLOUDFLARE_PUBLIC_HOSTNAME_QUICK_FIX.md)
- [ไม่ต้องซื้อ Domain](NO_DOMAIN_NEEDED.md)

---

<div align="center">
✅ **Login สำเร็จแล้ว! ทำขั้นตอนต่อไปได้เลย**

🚀 **ขั้นตอนต่อไป: สร้าง Tunnel**
</div>

