# ⚡ แก้ไขปัญหา: npm error Missing script: "dev"

## 🔍 สาเหตุ

คุณรัน `npm run dev` ในโฟลเดอร์ที่ผิด (ไม่ได้อยู่ในโฟลเดอร์โปรเจกต์)

---

## ✅ วิธีแก้ไข (3 วิธี)

### วิธีที่ 1: ใช้ PowerShell Script (แนะนำ - รองรับ path ที่มีช่องว่าง)

**เปิด PowerShell แล้วรัน:**
```powershell
.\START_FRONTEND.ps1
```

**หรือดับเบิลคลิก:** `START_FRONTEND.ps1`

---

### วิธีที่ 1b: ใช้ Batch File (ถ้า PowerShell ไม่ทำงาน)

**เปิด Command Prompt (cmd) แล้วรัน:**
```cmd
START_FRONTEND.bat
```

**หรือใช้แบบง่าย:**
```cmd
START_FRONTEND_SIMPLE.bat
```

---

### วิธีที่ 2: เปลี่ยนโฟลเดอร์ก่อน (Manual)

**เปิด PowerShell/Terminal:**

```powershell
# เปลี่ยนไปที่โฟลเดอร์โปรเจกต์
cd "C:\Users\jay_rpn\Documents\Project Big\Project Pos Chat\line chat"

# ตรวจสอบว่า package.json มีอยู่
Test-Path package.json

# เริ่มต้น Frontend
npm run dev
```

---

### วิธีที่ 3: ใช้ START_SERVERS.bat (เริ่มต้นทั้ง Backend + Frontend)

**ดับเบิลคลิก:**
```
START_SERVERS.bat
```

---

## 🔍 ตรวจสอบ

### 1. ตรวจสอบว่า package.json มีอยู่

```powershell
# อยู่ในโฟลเดอร์โปรเจกต์
cd "C:\Users\jay_rpn\Documents\Project Big\Project Pos Chat\line chat"

# ตรวจสอบ
Test-Path package.json
# ควรเห็น: True
```

### 2. ตรวจสอบว่า script "dev" มีอยู่

```powershell
# ดู scripts ใน package.json
Get-Content package.json | Select-String "dev"
# ควรเห็น: "dev": "vite"
```

### 3. ตรวจสอบว่า Frontend ทำงาน

```powershell
# ตรวจสอบ port 8085
netstat -ano | findstr ":8085"
# ควรเห็น: LISTENING
```

---

## 📋 Checklist

- [ ] อยู่ในโฟลเดอร์โปรเจกต์ที่ถูกต้อง
- [ ] package.json มีอยู่
- [ ] script "dev" มีอยู่ใน package.json
- [ ] node_modules ติดตั้งแล้ว
- [ ] Frontend server ทำงาน (port 8085)

---

## 🚀 ขั้นตอนต่อไป

1. **รัน START_FRONTEND.bat** (หรือ START_SERVERS.bat)
2. **รอให้เห็น:**
   ```
   VITE v7.x.x  ready in xxx ms
   ➜  Local:   http://localhost:8085/
   ```
3. **เปิดเบราว์เซอร์:** http://localhost:8085

---

## 💡 Tips

- **ใช้ Batch Files** - ไม่ต้องจำ path
- **ตรวจสอบโฟลเดอร์** - ดู path ใน Terminal
- **อ่าน Error Message** - มักบอกสาเหตุชัดเจน

---

**แก้ไขเสร็จแล้ว! 🎉**

