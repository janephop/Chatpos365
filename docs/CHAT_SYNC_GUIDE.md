# 💬 คู่มือการ Sync ประวัติการคุยระหว่างเครื่อง

## 📋 ภาพรวม

ระบบแชทเก็บข้อมูลการคุยทั้งหมดใน:
- **Local Storage:** JSON files + SQLite database
- **Offline-First:** ทำงานได้แม้ไม่มีอินเทอร์เน็ต (ยกเว้นส่งข้อความผ่าน LINE)
- **Cross-Device:** เครื่องอื่นสามารถดึงประวัติการคุยได้

---

## 🗄️ การเก็บข้อมูล

### ไฟล์ที่เก็บข้อมูลแชท

1. **JSON Files:**
   - `line-webhook/data/chats.json` - ข้อมูลแชท
   - `line-webhook/data/messages.json` - ข้อความทั้งหมด

2. **SQLite Database:**
   - `line-webhook/data/pos_chat.db`
   - ตาราง `chats` - ข้อมูลแชท
   - ตาราง `messages` - ข้อความทั้งหมด

### Auto Sync

- **JSON → Database:** เมื่อเริ่มโปรแกรม
- **Database → JSON:** เมื่อมีการเปลี่ยนแปลงผ่าน API
- **Memory → JSON + Database:** เมื่อรับ/ส่งข้อความ

---

## 📤 API สำหรับดึงประวัติการคุย

### 1. ดึงรายการแชททั้งหมด

**Endpoint:**
```
GET /api/chats
```

**Response:**
```json
[
  {
    "userId": "U1234567890",
    "name": "ลูกค้า 1",
    "avatar": "https://...",
    "platform": "line",
    "online": true,
    "time": "14:30",
    "unread": 2,
    "isPinned": false,
    "tags": [],
    "messageCount": 15,
    "lastMessage": "สวัสดีครับ"
  }
]
```

**คุณสมบัติ:**
- ดึงจาก Database (ถ้ามี) หรือ Memory
- เหมาะสำหรับเครื่องอื่นที่ต้องการดึงรายการแชท

---

### 2. ดึงประวัติการคุยของแชทหนึ่ง

**Endpoint:**
```
GET /api/chats/:userId/messages
```

**Query Parameters:**
- `limit` - จำนวนข้อความสูงสุด (optional)
- `offset` - ข้ามข้อความ (optional)
- `fromTimestamp` - ดึงข้อความตั้งแต่ timestamp นี้ (optional)

**ตัวอย่าง:**
```
GET /api/chats/U1234567890/messages?limit=50&offset=0
GET /api/chats/U1234567890/messages?fromTimestamp=1234567890
```

**Response:**
```json
[
  {
    "id": "msg_123",
    "text": "สวัสดีครับ",
    "sender": "user",
    "type": "text",
    "time": "14:30",
    "timestamp": 1234567890,
    "imageUrl": null,
    "videoUrl": null,
    "audioUrl": null,
    "fileUrl": null,
    "fileName": null
  }
]
```

**คุณสมบัติ:**
- ดึงจาก Database (ถ้ามี) หรือ Memory
- รองรับ Pagination
- รองรับ Filter by timestamp

---

### 3. ดึงประวัติการคุยทั้งหมด (สำหรับเครื่องอื่น)

**Endpoint:**
```
GET /api/chats/history/all
```

**Query Parameters:**
- `limit` - จำนวนข้อความสูงสุดต่อ user (optional)
- `offset` - ข้ามข้อความ (optional)
- `fromTimestamp` - ดึงข้อความตั้งแต่ timestamp นี้ (optional)

**Response:**
```json
{
  "U1234567890": [
    {
      "id": "msg_123",
      "text": "สวัสดีครับ",
      "sender": "user",
      "type": "text",
      "time": "14:30",
      "timestamp": 1234567890
    }
  ],
  "U0987654321": [
    {
      "id": "msg_456",
      "text": "ขอบคุณครับ",
      "sender": "admin",
      "type": "text",
      "time": "15:00",
      "timestamp": 1234567900
    }
  ]
}
```

**ใช้สำหรับ:**
- เครื่องอื่นต้องการดึงประวัติการคุยทั้งหมด
- Backup ข้อมูลแชท
- ย้ายข้อมูลไปเครื่องอื่น

---

### 4. Sync ข้อมูลแชททั้งหมด (Chats + Messages)

**Endpoint:**
```
GET /api/chats/sync/all
```

**Response:**
```json
{
  "chats": [
    {
      "userId": "U1234567890",
      "name": "ลูกค้า 1",
      "avatar": "https://...",
      "platform": "line",
      "online": true,
      "time": "14:30",
      "unread": 2,
      "isPinned": false,
      "tags": []
    }
  ],
  "messages": {
    "U1234567890": [
      {
        "id": "msg_123",
        "text": "สวัสดีครับ",
        "sender": "user",
        "type": "text",
        "time": "14:30",
        "timestamp": 1234567890
      }
    ]
  },
  "timestamp": 1234567890
}
```

**ใช้สำหรับ:**
- Sync ข้อมูลระหว่างเครื่อง
- Backup ข้อมูลแชททั้งหมด
- ย้ายข้อมูลไปเครื่องอื่น

---

## 📥 API สำหรับ Import ประวัติการคุย

### Import ประวัติการคุย

**Endpoint:**
```
POST /api/chats/sync/import
```

**Request Body:**
```json
{
  "chats": [
    {
      "userId": "U1234567890",
      "name": "ลูกค้า 1",
      "avatar": "https://...",
      "platform": "line",
      "online": true,
      "time": "14:30",
      "unread": 2,
      "isPinned": false,
      "tags": []
    }
  ],
  "messages": {
    "U1234567890": [
      {
        "id": "msg_123",
        "text": "สวัสดีครับ",
        "sender": "user",
        "type": "text",
        "time": "14:30",
        "timestamp": 1234567890
      }
    ]
  }
}
```

**Response:**
```json
{
  "success": true,
  "message": "Chat history imported successfully",
  "imported": {
    "chats": 10,
    "messages": 5
  }
}
```

**คุณสมบัติ:**
- Import ข้อมูลไป Database และ JSON files
- Update ข้อมูลที่มีอยู่แล้ว (ไม่ซ้ำ)
- Sync อัตโนมัติหลัง import

---

## 📊 API สำหรับสถิติ

### ดูสถิติการแชท

**Endpoint:**
```
GET /api/chats/stats
```

**Response:**
```json
{
  "totalChats": 10,
  "totalMessages": 150,
  "totalUnread": 5,
  "chatsByPlatform": {
    "line": 10
  },
  "messagesByType": {
    "text": 100,
    "image": 30,
    "video": 10,
    "file": 10
  },
  "oldestMessage": 1234567890,
  "newestMessage": 1234567900
}
```

---

## 🔄 วิธี Sync ข้อมูลระหว่างเครื่อง

### วิธีที่ 1: ใช้ API Sync (แนะนำ)

**เครื่อง A (ส่งข้อมูล):**
```bash
# 1. ดึงข้อมูลทั้งหมด
curl http://localhost:3000/api/chats/sync/all > chat_backup.json

# หรือเปิดเบราว์เซอร์
http://localhost:3000/api/chats/sync/all
```

**เครื่อง B (รับข้อมูล):**
```bash
# 1. Import ข้อมูล
curl -X POST http://localhost:3000/api/chats/sync/import \
  -H "Content-Type: application/json" \
  -d @chat_backup.json
```

---

### วิธีที่ 2: ใช้ Database File

**เครื่อง A:**
```bash
# 1. Export database
http://localhost:3000/api/export/database

# 2. ส่งไฟล์ pos_chat.db ไปเครื่อง B
```

**เครื่อง B:**
```bash
# 1. วางไฟล์ pos_chat.db ไปที่ line-webhook/data/
# 2. Restart server
# 3. ข้อมูลจะ sync ไป JSON files อัตโนมัติ
```

---

### วิธีที่ 3: ใช้ JSON Files

**เครื่อง A:**
```bash
# 1. Copy ไฟล์ JSON
copy line-webhook\data\chats.json
copy line-webhook\data\messages.json
```

**เครื่อง B:**
```bash
# 1. วางไฟล์ JSON ไปที่ line-webhook/data/
# 2. Restart server
# 3. ข้อมูลจะ sync ไป database อัตโนมัติ
```

---

## 📝 ตัวอย่างการใช้งาน

### ตัวอย่าง 1: ดึงประวัติการคุยทั้งหมด

```javascript
// Frontend หรือเครื่องอื่น
const response = await fetch('http://localhost:3000/api/chats/sync/all');
const data = await response.json();

console.log('Chats:', data.chats);
console.log('Messages:', data.messages);
```

### ตัวอย่าง 2: Import ประวัติการคุย

```javascript
const chatData = {
  chats: [...],
  messages: {...}
};

const response = await fetch('http://localhost:3000/api/chats/sync/import', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(chatData)
});

const result = await response.json();
console.log('Imported:', result);
```

### ตัวอย่าง 3: ดึงประวัติการคุยแบบ Pagination

```javascript
// ดึง 50 ข้อความล่าสุด
const response = await fetch(
  'http://localhost:3000/api/chats/U1234567890/messages?limit=50&offset=0'
);
const messages = await response.json();
```

---

## 🔒 Offline-First

### หลักการ

- **ข้อมูลถูกเก็บในเครื่อง** (ไม่ต้องเชื่อมต่ออินเทอร์เน็ต)
- **JSON Files** เป็น source of truth หลัก
- **SQLite Database** เป็น secondary storage
- **Memory (Map)** ใช้สำหรับการทำงานเร็ว

### การทำงาน

1. **เมื่อเริ่มโปรแกรม:**
   - โหลด JSON files → Memory
   - Sync JSON → SQLite Database

2. **เมื่อรับ/ส่งข้อความ:**
   - บันทึก Memory → JSON files
   - Sync JSON → SQLite Database

3. **เมื่อเครื่องอื่นดึงข้อมูล:**
   - ดึงจาก SQLite Database (ถ้ามี)
   - Fallback ไป Memory (ถ้าไม่มี database)

---

## ✅ Checklist

- [ ] ข้อมูลแชทถูกเก็บใน JSON files
- [ ] ข้อมูลแชทถูกเก็บใน SQLite database
- [ ] API ดึงประวัติการคุยทำงานได้
- [ ] API import ประวัติการคุยทำงานได้
- [ ] ทดสอบ sync ข้อมูลระหว่างเครื่อง
- [ ] ทดสอบ offline mode

---

## 🆘 Troubleshooting

### ปัญหา: เครื่องอื่นดึงข้อมูลไม่ได้

**แก้ไข:**
- ตรวจสอบว่า server ยังทำงานอยู่
- ตรวจสอบว่า database ถูกสร้างแล้ว
- ตรวจสอบ CORS settings

### ปัญหา: ข้อมูลไม่ sync

**แก้ไข:**
- เรียก API `/api/database/sync` เพื่อ sync manual
- Restart server
- ตรวจสอบว่า JSON files ถูกต้อง

### ปัญหา: ข้อมูลซ้ำ

**แก้ไข:**
- ระบบใช้ `INSERT OR REPLACE` ไม่ซ้ำ
- ถ้ายังซ้ำ ให้ลบข้อมูลเก่าก่อน import

---

## 📚 อ่านเพิ่มเติม

- [DATABASE_GUIDE.md](./DATABASE_GUIDE.md) - คู่มือการใช้งานฐานข้อมูล
- [CHAT_SYSTEM.md](./CHAT_SYSTEM.md) - ระบบแชททั้งหมด

