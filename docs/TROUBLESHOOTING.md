# 🔧 คู่มือแก้ไขปัญหา "Failed to fetch" - Backend ไม่ตอบสนอง

## 🚨 ปัญหา: Frontend ไม่สามารถเชื่อมต่อกับ Backend

### อาการ
- Console แสดง: `❌ Cannot connect to backend at https://chatpos365-production.up.railway.app`
- Error: `TypeError: Failed to fetch`
- Frontend ไม่สามารถโหลด chats/messages ได้

---

## ✅ วิธีแก้ไข (ทำตามลำดับ)

### ขั้นตอนที่ 1: ตรวจสอบ Backend Service Status

1. **ไปที่ Railway Dashboard**
   - เปิด: https://railway.app
   - เลือก Project ที่มี Backend Service

2. **ตรวจสอบ Backend Service**
   - ดู Status:
     - ✅ **Active (สีเขียว)** = ทำงานปกติ
     - ⚠️ **Inactive (สีเทา)** = หยุดทำงาน → คลิก "Deploy" หรือ "Restart"
     - ❌ **Error (สีแดง)** = มีปัญหา → ดู Logs

3. **ถ้า Status = Inactive หรือ Error:**
   - คลิก "Deploy" หรือ "Restart"
   - รอให้ deploy เสร็จ (ประมาณ 2-5 นาที)

---

### ขั้นตอนที่ 2: ตรวจสอบ Backend Logs

1. **ไปที่ Backend Service → Logs**
2. **ตรวจสอบว่าเห็น:**
   ```
   ✅ LINE webhook server is running!
   📍 Local: http://localhost:3000
   ```

3. **ถ้าเห็น Error:**
   - อ่าน error message
   - แก้ไขตาม error message

4. **ถ้าไม่มี Logs:**
   - Backend อาจจะยังไม่ได้ deploy
   - คลิก "Deploy" เพื่อ deploy ใหม่

---

### ขั้นตอนที่ 3: ทดสอบ Backend URL โดยตรง

1. **เปิด Browser ใหม่ (Incognito/Private)**
2. **เปิด URL:** `https://chatpos365-production.up.railway.app/`
3. **ควรเห็น JSON response:**
   ```json
   {
     "status": "ok",
     "message": "LINE Webhook Server is running",
     ...
   }
   ```

4. **ถ้าไม่เห็น (404, 500, หรือ timeout):**
   - Backend ไม่ทำงาน
   - กลับไปขั้นตอนที่ 1-2

---

### ขั้นตอนที่ 4: ตรวจสอบ Environment Variables

1. **ไปที่ Backend Service → Variables**
2. **ตรวจสอบว่ามี:**
   - `PORT=3000` (หรือไม่ต้องตั้งก็ได้ Railway จะ inject อัตโนมัติ)
   - `NODE_ENV=production` (optional)

3. **ถ้าไม่มี → เพิ่ม Variables**

---

### ขั้นตอนที่ 5: ตรวจสอบ Build Settings

1. **ไปที่ Backend Service → Settings**
2. **ตรวจสอบ:**
   - **Root Directory:** `line-webhook` (สำคัญมาก!)
   - **Start Command:** `npm start` หรือ `node index.js`
   - **Build Command:** `npm install`

3. **ถ้า Root Directory ไม่ถูกต้อง:**
   - เปลี่ยนเป็น `line-webhook`
   - Save และ Redeploy

---

### ขั้นตอนที่ 6: ตรวจสอบ Frontend Variables

1. **ไปที่ Frontend Service → Variables**
2. **ตรวจสอบว่ามี:**
   - `VITE_API_URL=https://chatpos365-production.up.railway.app`
   - (ใช้ Backend URL จากขั้นตอนที่ 3)

3. **ถ้าไม่มีหรือไม่ถูกต้อง:**
   - เพิ่ม/แก้ไข Variable
   - Save และ Redeploy Frontend

---

## 🔍 สาเหตุที่พบบ่อย

### 1. Backend Service หลับ (Sleep Mode)
- **สาเหตุ:** Railway free tier จะ sleep เมื่อไม่มีการใช้งาน
- **แก้ไข:** 
  - คลิก "Deploy" หรือ "Restart" ใน Railway Dashboard
  - หรือส่ง request ไปที่ Backend URL เพื่อ wake up

### 2. Root Directory ไม่ถูกต้อง
- **สาเหตุ:** Railway ไม่รู้ว่าไฟล์อยู่ที่ไหน
- **แก้ไข:** ตั้งค่า Root Directory = `line-webhook`

### 3. Start Command ไม่ถูกต้อง
- **สาเหตุ:** Railway ไม่รู้ว่าจะรันคำสั่งอะไร
- **แก้ไข:** ตั้งค่า Start Command = `npm start` หรือ `node index.js`

### 4. VITE_API_URL ไม่ถูกตั้งค่า
- **สาเหตุ:** Frontend ไม่รู้ว่า Backend อยู่ที่ไหน
- **แก้ไข:** ตั้งค่า `VITE_API_URL` ใน Frontend Variables

### 5. Backend URL เปลี่ยน
- **สาเหตุ:** Railway อาจจะ generate domain ใหม่
- **แก้ไข:** 
  - ตรวจสอบ Backend URL ใหม่ใน Railway Dashboard
  - อัปเดต `VITE_API_URL` ใน Frontend Variables

---

## 🛠️ วิธีแก้ไขแบบละเอียด

### ถ้า Backend Service Status = Inactive

1. **คลิก "Deploy" หรือ "Restart"**
2. **รอให้ deploy เสร็จ**
3. **ตรวจสอบ Logs:**
   - ควรเห็น: `✅ LINE webhook server is running!`
4. **ทดสอบ Backend URL:**
   - เปิด: `https://chatpos365-production.up.railway.app/`
   - ควรเห็น JSON response

### ถ้า Backend Service Status = Error

1. **ดู Logs เพื่อหาสาเหตุ**
2. **แก้ไขตาม error message:**
   - **Module not found:** ตรวจสอบ `package.json` และ dependencies
   - **Port already in use:** ตรวจสอบ PORT variable
   - **Database error:** ตรวจสอบ database connection

### ถ้า Backend URL ไม่เปิดได้

1. **ตรวจสอบว่า Backend Service มี Domain หรือไม่:**
   - ไปที่ Backend Service → Settings → Networking
   - ถ้าไม่มี → Generate Domain

2. **ตรวจสอบว่า Domain ถูกต้อง:**
   - ควรเป็น: `https://xxxxx.up.railway.app`
   - ไม่ใช่: `http://` (ต้องเป็น HTTPS)

---

## 📋 Checklist

- [ ] Backend Service Status = Active
- [ ] Backend Logs แสดง "server is running"
- [ ] Backend URL เปิดได้ใน browser
- [ ] Root Directory = `line-webhook`
- [ ] Start Command = `npm start` หรือ `node index.js`
- [ ] VITE_API_URL ถูกตั้งค่าใน Frontend Variables
- [ ] Frontend Service ถูก redeploy หลังตั้งค่า VITE_API_URL

---

## 🆘 ถ้ายังไม่ได้

### วิธีที่ 1: Redeploy ทั้ง Backend และ Frontend

1. **Backend Service:**
   - คลิก "Deploy" หรือ "Redeploy"
   - รอให้ deploy เสร็จ

2. **Frontend Service:**
   - คลิก "Deploy" หรือ "Redeploy"
   - รอให้ deploy เสร็จ

### วิธีที่ 2: ตรวจสอบ Network

1. **ทดสอบ Backend URL ใน Browser:**
   - เปิด: `https://chatpos365-production.up.railway.app/`
   - ควรเห็น JSON response

2. **ทดสอบ Backend API:**
   - เปิด: `https://chatpos365-production.up.railway.app/api/chats`
   - ควรเห็น JSON array (อาจจะเป็น `[]` ถ้ายังไม่มี chats)

### วิธีที่ 3: ตรวจสอบ CORS

1. **Backend ควรมี CORS setup แล้ว** (มีในโค้ด)
2. **ถ้ายังมีปัญหา:**
   - ตรวจสอบ Logs ใน Backend
   - ดู error messages เกี่ยวกับ CORS

---

## 📞 ต้องการความช่วยเหลือ?

1. **ตรวจสอบ Logs ใน Railway Dashboard**
2. **ตรวจสอบ Environment Variables**
3. **ตรวจสอบ Network Settings**
4. **ลอง Redeploy ทั้ง Backend และ Frontend**

---

## 💡 Tips

- **Railway free tier จะ sleep เมื่อไม่มีการใช้งาน** → ต้อง wake up โดยส่ง request
- **ตรวจสอบ Logs เป็นประจำ** → จะเห็นปัญหาได้เร็ว
- **ใช้ Railway Variables สำหรับ configuration** → ไม่ต้อง hardcode
- **Monitor Backend Service Status** → จะรู้ได้ทันทีถ้ามีปัญหา

