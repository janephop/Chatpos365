# 🚀 คู่มือตั้งค่า: อะไรทำเอง vs อะไร AI ทำให้

## ✅ สิ่งที่ AI ทำให้แล้ว (ไม่ต้องทำเอง)

### 1. ไฟล์และสคริปต์
- ✅ สคริปต์ตั้งค่า: `SETUP_PERMANENT_TUNNEL.bat`
- ✅ สคริปต์เริ่มต้น: `START_PERMANENT.bat`
- ✅ ไฟล์ config template: `cloudflare-tunnel-config.yml.example`
- ✅ ไฟล์ .env template: `.env.example`, `line-webhook/.env.example`
- ✅ เอกสารทั้งหมด

### 2. Dependencies
- ✅ Frontend dependencies ติดตั้งแล้ว
- ✅ Backend dependencies ติดตั้งแล้ว

### 3. โครงสร้างโปรเจกต์
- ✅ โค้ดทั้งหมดพร้อมใช้งาน
- ✅ Database structure พร้อม

---

## 🔴 สิ่งที่คุณต้องทำเอง (ทำตามลำดับ)

### ขั้นตอนที่ 1: ตั้งค่า Cloudflare Tunnel (10 นาที)

#### 1.1 สร้าง Cloudflare Account (ถ้ายังไม่มี)
- ไปที่: https://dash.cloudflare.com/sign-up
- สร้างบัญชีฟรี (ไม่ต้องใส่บัตรเครดิต)
- **เวลา: 2 นาที**

#### 1.2 Login และสร้าง Tunnel
```bash
# ดับเบิลคลิกไฟล์นี้
SETUP_PERMANENT_TUNNEL.bat
```

สคริปต์จะทำ:
1. Login เข้า Cloudflare (จะเปิดเบราว์เซอร์ให้ login)
2. สร้าง Tunnel ชื่อ "line-chat"
3. สร้างไฟล์ config

**สิ่งที่คุณต้องทำ:**
- Login ในเบราว์เซอร์ที่เปิดขึ้นมา
- จด Tunnel ID ที่แสดง (ถ้ามี)

**เวลา: 5 นาที**

---

### ขั้นตอนที่ 2: ตั้งค่า Public Hostname ใน Cloudflare Dashboard (10 นาที)

#### 2.1 ไปที่ Cloudflare Dashboard
1. ไปที่: https://one.dash.cloudflare.com/
2. Login เข้าระบบ
3. เลือก **Zero Trust** → **Networks** → **Tunnels**
4. คลิก tunnel **"line-chat"**

#### 2.2 เพิ่ม Public Hostname สำหรับ Backend
1. ไปที่แท็บ **"Public Hostnames"**
2. คลิก **"Add a public hostname"**
3. กรอกข้อมูล:
   - **Subdomain**: `line-chat-backend-12345` (เปลี่ยน 12345 เป็นตัวเลขที่ต้องการ)
   - **Domain**: เลือก `trycloudflare.com`
   - **Service**: `http://localhost:3000`
4. คลิก **"Add hostname"**
5. **จด URL ที่ได้ไว้** (เช่น: `https://line-chat-backend-12345.trycloudflare.com`)

#### 2.3 เพิ่ม Public Hostname สำหรับ Frontend
1. คลิก **"Add a public hostname"** อีกครั้ง
2. กรอกข้อมูล:
   - **Subdomain**: `line-chat-frontend-12345` (ใช้ตัวเลขเดียวกันกับ Backend)
   - **Domain**: เลือก `trycloudflare.com`
   - **Service**: `http://localhost:8085`
3. คลิก **"Add hostname"**
4. **จด URL ที่ได้ไว้** (เช่น: `https://line-chat-frontend-12345.trycloudflare.com`)

**เวลา: 10 นาที**

---

### ขั้นตอนที่ 3: แก้ไขไฟล์ Config (2 นาที)

#### 3.1 คัดลอกไฟล์ config template
```bash
# ถ้ายังไม่มีไฟล์ cloudflare-tunnel-config.yml
# สคริปต์ SETUP_PERMANENT_TUNNEL.bat จะสร้างให้อัตโนมัติ
```

#### 3.2 แก้ไข hostname ในไฟล์
1. เปิดไฟล์ `cloudflare-tunnel-config.yml`
2. แก้ไข hostname ให้ตรงกับที่ตั้งใน Dashboard:
   ```yaml
   tunnel: <TUNNEL_ID>  # ไม่ต้องแก้ (สคริปต์จะใส่ให้)
   credentials-file: .cloudflared\<TUNNEL_ID>.json
   
   ingress:
     - hostname: line-chat-backend-12345.trycloudflare.com  # ← แก้ไขตรงนี้
       service: http://localhost:3000
     - hostname: line-chat-frontend-12345.trycloudflare.com  # ← แก้ไขตรงนี้
       service: http://localhost:8085
     - service: http_status:404
   ```

**เวลา: 2 นาที**

---

### ขั้นตอนที่ 4: สร้างไฟล์ .env (3 นาที)

#### 4.1 สร้างไฟล์ .env สำหรับ Frontend
1. คัดลอกไฟล์ `.env.example` เป็น `.env`
2. แก้ไขค่า:
   ```env
   VITE_API_URL=https://line-chat-backend-12345.trycloudflare.com
   ```
   (เปลี่ยน 12345 ให้ตรงกับ Backend URL ที่ตั้งไว้)

#### 4.2 สร้างไฟล์ .env สำหรับ Backend
1. ไปที่โฟลเดอร์ `line-webhook`
2. คัดลอกไฟล์ `.env.example` เป็น `.env`
3. แก้ไขค่า:
   ```env
   LINE_CHANNEL_ACCESS_TOKEN=your_channel_access_token_here
   LINE_CHANNEL_SECRET=your_channel_secret_here
   PORT=3000
   ```
   
   **หา Token และ Secret ได้จาก:**
   - ไปที่: https://developers.line.biz/console/
   - เลือก Provider > Channel
   - **Messaging API** tab
   - **Channel Access Token**: คลิก "Issue" หรือ "Reissue"
   - **Channel Secret**: ดูใน Basic settings

**เวลา: 3 นาที**

---

### ขั้นตอนที่ 5: เริ่มต้นระบบ (1 นาที)

```bash
# ดับเบิลคลิกไฟล์นี้
START_PERMANENT.bat
```

สคริปต์จะ:
1. เริ่ม Backend Server (port 3000)
2. เริ่ม Frontend Server (port 8085)
3. เริ่ม Cloudflare Tunnel

**เวลา: 1 นาที**

---

### ขั้นตอนที่ 6: ตั้งค่า LINE Webhook (2 นาที)

1. ไปที่: https://developers.line.biz/console/
2. เลือก Provider > Channel > **Messaging API** tab
3. **Webhook settings** > Edit
4. ใส่ Backend URL + `/webhook/line`:
   ```
   https://line-chat-backend-12345.trycloudflare.com/webhook/line
   ```
5. เปิด **Use webhook** = Enabled
6. กด **Verify**

**เวลา: 2 นาที**

---

## ✅ เสร็จแล้ว!

ตอนนี้ระบบพร้อมใช้งาน:
- ✅ Backend URL: `https://line-chat-backend-12345.trycloudflare.com`
- ✅ Frontend URL: `https://line-chat-frontend-12345.trycloudflare.com`
- ✅ URL คงที่ถาวร - ไม่ต้องเปลี่ยน webhook!

---

## 📋 Checklist

- [ ] สร้าง Cloudflare Account
- [ ] รัน SETUP_PERMANENT_TUNNEL.bat
- [ ] ตั้งค่า Public Hostname ใน Dashboard (Backend)
- [ ] ตั้งค่า Public Hostname ใน Dashboard (Frontend)
- [ ] แก้ไข cloudflare-tunnel-config.yml
- [ ] สร้างไฟล์ .env (Frontend)
- [ ] สร้างไฟล์ .env (Backend)
- [ ] ใส่ LINE Channel Access Token และ Secret
- [ ] รัน START_PERMANENT.bat
- [ ] ตั้งค่า LINE Webhook

---

## 🆘 ถ้ามีปัญหา

### ปัญหา: Tunnel ไม่ทำงาน
- ตรวจสอบว่า Backend และ Frontend รันอยู่
- ตรวจสอบไฟล์ cloudflare-tunnel-config.yml
- ตรวจสอบว่า hostname ตรงกับ Dashboard

### ปัญหา: Frontend ไม่เชื่อมต่อ Backend
- ตรวจสอบ VITE_API_URL ใน .env
- Restart Frontend

### ปัญหา: LINE Webhook ไม่ทำงาน
- ตรวจสอบว่า Backend Tunnel ยังทำงานอยู่
- ตรวจสอบ URL ใน LINE Developers Console
- ตรวจสอบว่าใช้ HTTPS (ไม่ใช่ HTTP)

---

## 📚 อ่านเพิ่มเติม

- [คู่มือฉบับเต็ม](PERMANENT_TUNNEL_GUIDE.md)
- [สถาปัตยกรรมระบบ](ARCHITECTURE.md)
- [Quick Start](QUICK_START_PERMANENT.md)

