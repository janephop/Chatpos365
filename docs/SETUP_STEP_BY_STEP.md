# 📖 คู่มือตั้งค่าแบบละเอียดทุกขั้นตอน (Step-by-Step)

> คู่มือนี้จะบอกทุกขั้นตอนแบบละเอียด ว่าต้องกดตรงไหน ไปตรงไหน พร้อมลิงก์ทุกขั้นตอน

## ⚠️ สิ่งสำคัญที่ต้องรู้ก่อนเริ่ม

### ✅ ไม่ต้องซื้อ Domain!
- **ใช้ `trycloudflare.com` ได้เลย (ฟรี 100%)**
- ไม่ต้องมี Domain ของตัวเอง
- เลือก `trycloudflare.com` ใน dropdown เมื่อตั้งค่า Public Hostname

### 🔍 ถ้าไม่เห็น Public Hostnames
- **วิธีที่ 1:** ใช้ URL โดยตรง - เพิ่ม `/public-hostnames` ต่อท้าย URL
- **วิธีที่ 2:** ตรวจสอบ Status - Tunnel ต้องเป็น "Healthy" หรือ "Active"
- **วิธีที่ 3:** กลับไป Tunnel List แล้วเข้าใหม่

---

## 📋 สารบัญ

1. [ขั้นตอนที่ 1: สร้าง Cloudflare Account](#ขั้นตอนที่-1-สร้าง-cloudflare-account)
2. [ขั้นตอนที่ 2: ดาวน์โหลด Cloudflare Tunnel](#ขั้นตอนที่-2-ดาวน์โหลด-cloudflare-tunnel)
3. [ขั้นตอนที่ 3: ตั้งค่า Cloudflare Tunnel](#ขั้นตอนที่-3-ตั้งค่า-cloudflare-tunnel)
4. [ขั้นตอนที่ 4: ตั้งค่า Public Hostname ใน Dashboard](#ขั้นตอนที่-4-ตั้งค่า-public-hostname-ใน-dashboard)
5. [ขั้นตอนที่ 5: แก้ไขไฟล์ Config](#ขั้นตอนที่-5-แก้ไขไฟล์-config)
6. [ขั้นตอนที่ 6: สร้างไฟล์ .env](#ขั้นตอนที่-6-สร้างไฟล์-env)
7. [ขั้นตอนที่ 7: หา LINE Credentials](#ขั้นตอนที่-7-หา-line-credentials)
8. [ขั้นตอนที่ 8: เริ่มต้นระบบ](#ขั้นตอนที่-8-เริ่มต้นระบบ)
9. [ขั้นตอนที่ 9: ตั้งค่า LINE Webhook](#ขั้นตอนที่-9-ตั้งค่า-line-webhook)
10. [Troubleshooting](#troubleshooting)

---

## ขั้นตอนที่ 1: สร้าง Cloudflare Account

### 1.1 เปิดเว็บไซต์ Cloudflare

1. เปิดเบราว์เซอร์ (Chrome, Edge, Firefox)
2. ไปที่ลิงก์นี้: **https://dash.cloudflare.com/sign-up**
3. หรือค้นหา "Cloudflare sign up" ใน Google

### 1.2 สร้างบัญชี

1. **กรอก Email**:
   - ใส่ Email ที่ต้องการใช้ (เช่น: yourname@gmail.com)
   - คลิก **"Continue"** หรือกด Enter

2. **สร้าง Password**:
   - ใส่ Password ที่ต้องการ
   - Password ต้องมีอย่างน้อย 8 ตัวอักษร
   - คลิก **"Create Account"**

3. **ยืนยัน Email** (ถ้ามี):
   - ตรวจสอบ Email ที่กรอก
   - คลิกลิงก์ยืนยันใน Email (ถ้ามี)

### 1.3 เข้าสู่ระบบ

1. ไปที่: **https://dash.cloudflare.com/login**
2. กรอก Email และ Password
3. คลิก **"Log in"**

**✅ เสร็จแล้ว!** ตอนนี้คุณมี Cloudflare Account แล้ว

---

## ขั้นตอนที่ 2: ดาวน์โหลด Cloudflare Tunnel

### 2.1 เปิดหน้า Download

1. เปิดเบราว์เซอร์
2. ไปที่ลิงก์นี้: **https://github.com/cloudflare/cloudflared/releases**
3. หรือค้นหา "cloudflared releases github" ใน Google

### 2.2 หาไฟล์สำหรับ Windows

1. **เลื่อนลง** ไปที่ส่วน "Assets" (ถ้าไม่เห็น ให้คลิก "Assets" เพื่อขยาย)
2. **หาไฟล์**: `cloudflared-windows-amd64.exe`
3. **คลิกขวา** ที่ไฟล์ `cloudflared-windows-amd64.exe`
4. เลือก **"Save link as..."** หรือ **"Save target as..."**

### 2.3 บันทึกไฟล์

1. **เลือกโฟลเดอร์** ที่ต้องการบันทึก (แนะนำ: Desktop หรือ Downloads)
2. **เปลี่ยนชื่อไฟล์** เป็น: `cloudflared.exe`
   - ลบ `-windows-amd64` ออก
   - เหลือแค่ `cloudflared.exe`
3. คลิก **"Save"**

### 2.4 ย้ายไฟล์ไปโฟลเดอร์โปรเจกต์

1. **เปิด File Explorer** (กด Windows + E)
2. ไปที่โฟลเดอร์โปรเจกต์:
   ```
   C:\Users\jay_rpn\Documents\Project Big\Project Pos Chat\line chat
   ```
3. **Copy ไฟล์** `cloudflared.exe` จาก Desktop/Downloads
4. **Paste** ลงในโฟลเดอร์โปรเจกต์

**✅ เสร็จแล้ว!** ตอนนี้มีไฟล์ `cloudflared.exe` ในโฟลเดอร์โปรเจกต์แล้ว

---

## ขั้นตอนที่ 3: ตั้งค่า Cloudflare Tunnel

### 3.1 เปิด Command Prompt หรือ PowerShell

**วิธีที่ 1: ใช้ File Explorer**
1. เปิด File Explorer
2. ไปที่โฟลเดอร์โปรเจกต์
3. **คลิกที่แถบ Address bar** (ที่แสดง path)
4. พิมพ์: `cmd` แล้วกด Enter
   - หรือพิมพ์: `powershell` แล้วกด Enter

**วิธีที่ 2: ใช้ Run**
1. กด **Windows + R**
2. พิมพ์: `cmd` แล้วกด Enter

### 3.2 เปลี่ยนไปที่โฟลเดอร์โปรเจกต์

ใน Command Prompt หรือ PowerShell พิมพ์:

```cmd
cd "C:\Users\jay_rpn\Documents\Project Big\Project Pos Chat\line chat"
```

กด Enter

### 3.3 Login เข้า Cloudflare

พิมพ์คำสั่ง:

```cmd
cloudflared.exe tunnel login
```

กด Enter

**สิ่งที่เกิดขึ้น:**
1. จะเปิดเบราว์เซอร์อัตโนมัติ
2. ถ้าไม่เปิด ให้ Copy URL ที่แสดงใน Terminal แล้วเปิดในเบราว์เซอร์เอง

**ในเบราว์เซอร์:**
1. **Login** เข้า Cloudflare (ถ้ายังไม่ได้ login)
2. จะเห็นหน้า **"Authorize Cloudflare Tunnel"**
3. **สำคัญ:** หน้านี้จะถามให้เลือก Zone (domain)
   - **ถ้าไม่มี Zone ให้เลือก:** 
     - **มองหาปุ่ม "Skip"** หรือ **"Continue without zone"** หรือ **"Skip this step"**
     - **คลิกปุ่ม Skip** เพื่อข้ามไป
     - **หรือ:** ปิดหน้าต่างนี้ แล้วกลับไปที่ Terminal
   - **ถ้ามี Zone:** เลือก Zone ที่ต้องการ (แต่สำหรับ trycloudflare.com ไม่จำเป็น)
4. **ถ้าเห็นปุ่ม "Authorize"** หรือ **"Allow"** → **คลิก**
5. จะเห็นข้อความ "Success!" หรือ "Authorized"

**หมายเหตุ:** สำหรับการใช้ trycloudflare.com subdomain **ไม่จำเป็นต้องมี Zone** ใน Cloudflare Account สามารถข้ามขั้นตอนเลือก Zone ได้เลย

**กลับไปที่ Terminal:**
- จะเห็นข้อความ "You have successfully logged in"
- **✅ Login สำเร็จ!**

### 3.4 สร้าง Tunnel

พิมพ์คำสั่ง:

```cmd
cloudflared.exe tunnel create line-chat
```

กด Enter

**สิ่งที่เกิดขึ้น:**
- จะแสดง Tunnel ID (เป็นตัวอักษรยาวๆ เช่น: `a1b2c3d4-e5f6-7890-abcd-ef1234567890`)
- **จด Tunnel ID ไว้** (หรือไม่จดก็ได้ เพราะเก็บไว้ในไฟล์แล้ว)

**✅ Tunnel สร้างสำเร็จ!**

---

## ขั้นตอนที่ 4: ตั้งค่า Public Hostname ใน Dashboard

### 4.1 เปิด Cloudflare Dashboard

1. เปิดเบราว์เซอร์
2. ไปที่ลิงก์นี้: **https://one.dash.cloudflare.com/**
3. **Login** เข้าระบบ (ถ้ายังไม่ได้ login)

### 4.2 ไปที่หน้า Tunnels

1. **คลิกเมนูด้านซ้าย**: **"Zero Trust"**
   - ถ้าไม่เห็น ให้คลิก **"More products"** หรือ **"All products"** แล้วหา "Zero Trust"
2. **คลิก**: **"Networks"** (ในเมนูด้านซ้าย)
3. **คลิก**: **"Tunnels"** (ในเมนูด้านซ้าย)

### 4.3 หา Tunnel ที่สร้างไว้

1. **มองหา** tunnel ชื่อ **"line-chat"** ในรายการ
2. **คลิก** ที่ tunnel "line-chat"

### 4.4 ไปที่แท็บ Public Hostnames

**วิธีที่ 1: หาแท็บ (ปกติ)**
1. **มองหาที่ด้านบน** ของหน้า (ใต้ชื่อ tunnel "line-chat")
2. **จะเห็นแท็บต่างๆ เช่น:**
   - "Overview" (หรือ "Details")
   - **"Public Hostnames"** ← **นี่คือแท็บที่ต้องการ!**
   - "Private Networks" (ถ้ามี)
   - "Routes" (ถ้ามี)
   - "Settings" (ถ้ามี)
3. **คลิกแท็บ**: **"Public Hostnames"**

**วิธีที่ 2: ใช้ URL โดยตรง (ถ้าไม่เห็นแท็บ)**
1. **Copy URL** จาก address bar (หน้า Tunnel Detail)
2. **เพิ่ม** `/public-hostnames` ต่อท้าย URL
3. **ตัวอย่าง:**
   ```
   https://one.dash.cloudflare.com/.../tunnels/line-chat/public-hostnames
   ```
4. **Paste** ใน address bar แล้วกด Enter

**วิธีที่ 3: ตรวจสอบ Status (ถ้ายังไม่เห็น)**
1. **กลับไปที่หน้า Tunnel List**
2. **ดู Status** ของ tunnel "line-chat"
3. **ต้องเป็น "Healthy"** หรือ **"Active"**
4. **ถ้าเป็น "Inactive"**: ต้องรัน tunnel ก่อน
   ```cmd
   cloudflared.exe tunnel run line-chat --config cloudflare-tunnel-config.yml
   ```

### 4.5 เพิ่ม Public Hostname สำหรับ Backend

**⚠️ สำคัญ: เลือก `trycloudflare.com` (ไม่ต้องซื้อ Domain!)**

1. **คลิกปุ่ม**: **"Add a public hostname"** หรือ **"Add hostname"**

2. **กรอกฟอร์ม:**

   **Subdomain:**
   ```
   line-chat-backend-12345
   ```
   - เปลี่ยน `12345` เป็นตัวเลขที่ต้องการ (เช่น: `12345`, `99999`, `2024`)
   - ใช้ตัวอักษร/ตัวเลข/ขีดกลาง (-) เท่านั้น

   **Domain:**
   ```
   trycloudflare.com  ← เลือกอันนี้! (ฟรี ไม่ต้องซื้อ)
   ```
   - **คลิก dropdown** (ลูกศรลง)
   - **เลือก**: **`trycloudflare.com`**
   - **สำคัญ:** ไม่ต้องเลือก domain ของตัวเอง (ไม่ต้องซื้อ Domain)

   **Service:**
   ```
   http://localhost:3000
   ```
   - ต้องพิมพ์ให้ถูกต้อง: `http://localhost:3000` (ไม่ใช่ https)
   - **สำคัญ:** ต้องเป็น `localhost:3000` (ไม่ใช่ IP อื่น)

3. **คลิกปุ่ม**: **"Add hostname"** หรือ **"Save"**

4. **รอสักครู่** จนเห็น hostname แสดงในรายการ

**✅ Backend Hostname สร้างสำเร็จ!**

**จด URL ที่ได้ไว้:**
- จะเห็น URL แสดงในรายการ (เช่น: `https://line-chat-backend-12345.trycloudflare.com`)
- **Copy URL นี้ไว้** (คลิกขวา → Copy หรือ Select แล้ว Ctrl+C)
- **ตัวอย่าง:** `https://line-chat-backend-12345.trycloudflare.com`

### 4.6 เพิ่ม Public Hostname สำหรับ Frontend

1. **คลิกปุ่ม**: **"Add a public hostname"** อีกครั้ง

2. **กรอกฟอร์ม:**

   **Subdomain:**
   ```
   line-chat-frontend-12345
   ```
   - ใช้ตัวเลขเดียวกันกับ Backend (เช่น: ถ้า Backend เป็น `12345` Frontend ก็เป็น `12345`)

   **Domain:**
   ```
   trycloudflare.com  ← เลือกอันนี้! (ฟรี)
   ```
   - **คลิก dropdown**
   - **เลือก**: **`trycloudflare.com`**

   **Service:**
   ```
   http://localhost:8085
   ```
   - ต้องพิมพ์ให้ถูกต้อง: `http://localhost:8085`

3. **คลิกปุ่ม**: **"Add hostname"** หรือ **"Save"**

4. **รอสักครู่** จนเห็น hostname แสดงในรายการ

**✅ Frontend Hostname สร้างสำเร็จ!**

**จด URL ที่ได้ไว้:**
- จะเห็น URL แสดงในรายการ (เช่น: `https://line-chat-frontend-12345.trycloudflare.com`)
- **Copy URL นี้ไว้**
- **ตัวอย่าง:** `https://line-chat-frontend-12345.trycloudflare.com`

---

## ขั้นตอนที่ 5: แก้ไขไฟล์ Config

### 5.1 เปิดโฟลเดอร์โปรเจกต์

1. เปิด **File Explorer** (Windows + E)
2. ไปที่โฟลเดอร์:
   ```
   C:\Users\jay_rpn\Documents\Project Big\Project Pos Chat\line chat
   ```

### 5.2 หาไฟล์ Config

**ถ้ามีไฟล์ `cloudflare-tunnel-config.yml` อยู่แล้ว:**
- ข้ามไปขั้นตอน 5.3

**ถ้ายังไม่มี:**
1. **หาไฟล์**: `cloudflare-tunnel-config.yml.example`
2. **Copy ไฟล์** (คลิกขวา → Copy หรือ Ctrl+C)
3. **Paste** (คลิกขวา → Paste หรือ Ctrl+V)
4. **เปลี่ยนชื่อ** ไฟล์ที่ Copy มา:
   - คลิกขวาที่ไฟล์ → **Rename**
   - ลบ `.example` ออก
   - เหลือแค่: `cloudflare-tunnel-config.yml`

### 5.3 เปิดไฟล์ Config

1. **คลิกขวา** ที่ไฟล์ `cloudflare-tunnel-config.yml`
2. เลือก **"Open with"** → **"Notepad"** หรือ **"Notepad++"**
   - หรือดับเบิลคลิก (Windows จะถามว่าเปิดด้วยอะไร → เลือก Notepad)

### 5.4 แก้ไข Hostname

**หา Tunnel ID:**
1. ดูในไฟล์ จะเห็น:
   ```yaml
   tunnel: <TUNNEL_ID>
   ```
2. **ถ้ายังเป็น `<TUNNEL_ID>`:**
   - ต้องหา Tunnel ID ก่อน:
     - เปิด Command Prompt
     - พิมพ์: `cloudflared.exe tunnel list`
     - จะเห็น Tunnel ID (ตัวอักษรยาวๆ)
     - Copy Tunnel ID
   - **แทนที่** `<TUNNEL_ID>` ด้วย Tunnel ID ที่ Copy มา

**แก้ไข Hostname Backend:**
1. หาบรรทัด:
   ```yaml
   - hostname: line-chat-backend-xxxxx.trycloudflare.com
   ```
2. **แทนที่** `xxxxx` ด้วยตัวเลขที่ตั้งไว้ใน Dashboard
   - ตัวอย่าง: ถ้าใน Dashboard เป็น `line-chat-backend-12345.trycloudflare.com`
   - แก้เป็น:
     ```yaml
     - hostname: line-chat-backend-12345.trycloudflare.com
     ```

**แก้ไข Hostname Frontend:**
1. หาบรรทัด:
   ```yaml
   - hostname: line-chat-frontend-xxxxx.trycloudflare.com
   ```
2. **แทนที่** `xxxxx` ด้วยตัวเลขที่ตั้งไว้ใน Dashboard
   - ตัวอย่าง: ถ้าใน Dashboard เป็น `line-chat-frontend-12345.trycloudflare.com`
   - แก้เป็น:
     ```yaml
     - hostname: line-chat-frontend-12345.trycloudflare.com
     ```

### 5.5 บันทึกไฟล์

1. กด **Ctrl + S** (หรือ File → Save)
2. **ปิด** Notepad

**✅ แก้ไข Config เสร็จแล้ว!**

---

## ขั้นตอนที่ 6: สร้างไฟล์ .env

### 6.1 สร้างไฟล์ .env สำหรับ Frontend

1. เปิด **File Explorer**
2. ไปที่โฟลเดอร์โปรเจกต์
3. **คลิกขวา** ในพื้นที่ว่าง → **New** → **Text Document**
4. **เปลี่ยนชื่อ** เป็น: `.env`
   - **สำคัญ:** ต้องมีจุด (.) นำหน้า
   - ถ้า Windows ซ่อน extension:
     - ไปที่ View → Show → File name extensions (เปิด)
     - แล้วเปลี่ยนชื่อเป็น `.env` (ไม่มี .txt)

**ถ้า Windows ไม่ให้เปลี่ยนชื่อ:**
- ใช้วิธีนี้:
  1. สร้างไฟล์ชื่อ `env.txt`
  2. เปิด Command Prompt ในโฟลเดอร์นั้น
  3. พิมพ์: `ren env.txt .env`
  4. กด Enter

### 6.2 แก้ไขไฟล์ .env (Frontend)

1. **เปิดไฟล์** `.env` ด้วย Notepad
2. **พิมพ์** (หรือ Copy-Paste):
   ```env
   VITE_API_URL=https://line-chat-backend-12345.trycloudflare.com
   ```
3. **แทนที่** `12345` ด้วยตัวเลขที่ตั้งไว้ใน Dashboard
   - ตัวอย่าง: ถ้า Backend URL เป็น `https://line-chat-backend-99999.trycloudflare.com`
   - พิมพ์:
     ```env
     VITE_API_URL=https://line-chat-backend-99999.trycloudflare.com
     ```
4. **บันทึก** (Ctrl + S)
5. **ปิด** Notepad

**✅ Frontend .env เสร็จแล้ว!**

### 6.3 สร้างไฟล์ .env สำหรับ Backend

1. เปิด **File Explorer**
2. ไปที่โฟลเดอร์: `line-webhook` (ในโฟลเดอร์โปรเจกต์)
3. **คลิกขวา** → **New** → **Text Document**
4. **เปลี่ยนชื่อ** เป็น: `.env`

### 6.4 แก้ไขไฟล์ .env (Backend)

1. **เปิดไฟล์** `line-webhook\.env` ด้วย Notepad
2. **พิมพ์** (หรือ Copy-Paste):
   ```env
   LINE_CHANNEL_ACCESS_TOKEN=your_token_here
   LINE_CHANNEL_SECRET=your_secret_here
   PORT=3000
   ```
3. **ยังไม่ต้องแก้ไข** Token และ Secret ตอนนี้ (จะทำในขั้นตอนถัดไป)
4. **บันทึก** (Ctrl + S)
5. **ปิด** Notepad

**✅ Backend .env เสร็จแล้ว!** (แต่ยังต้องใส่ Token และ Secret)

---

## ขั้นตอนที่ 7: หา LINE Credentials

### 7.1 เปิด LINE Developers Console

1. เปิดเบราว์เซอร์
2. ไปที่ลิงก์นี้: **https://developers.line.biz/console/**
3. **Login** เข้าระบบ (ใช้ LINE Account)

### 7.2 เลือก Provider และ Channel

1. **คลิก** ที่ **Provider** (ด้านบนซ้าย)
   - ถ้ามีหลาย Provider ให้เลือก Provider ที่ต้องการ
2. **คลิก** ที่ **Channel** ที่ต้องการใช้
   - ถ้ายังไม่มี Channel:
     - คลิก **"Create"** หรือ **"New channel"**
     - เลือก **"Messaging API"**
     - กรอกข้อมูลตามที่ต้องการ
     - คลิก **"Create"**

### 7.3 หา Channel Access Token

1. **คลิกแท็บ**: **"Messaging API"** (ด้านบน)
2. **เลื่อนลง** ไปที่ส่วน **"Channel access token"**
3. **ดู** Token ที่แสดงอยู่:
   - ถ้ามี Token อยู่แล้ว → **Copy Token** (คลิกขวา → Copy หรือ Select แล้ว Ctrl+C)
   - ถ้ายังไม่มี Token:
     - คลิก **"Issue"** หรือ **"Generate"**
     - จะแสดง Token ขึ้นมา
     - **Copy Token** ทันที (เพราะจะแสดงแค่ครั้งเดียว!)

### 7.4 หา Channel Secret

1. **คลิกแท็บ**: **"Basic settings"** (ด้านบน)
2. **เลื่อนลง** ไปที่ส่วน **"Channel secret"**
3. **คลิกปุ่ม**: **"Show"** หรือ **"Reveal"** (ถ้า Token ถูกซ่อน)
4. **Copy Channel Secret** (คลิกขวา → Copy)

### 7.5 ใส่ Token และ Secret ในไฟล์ .env

1. เปิดไฟล์ `line-webhook\.env` ด้วย Notepad
2. **แทนที่** `your_token_here` ด้วย Token ที่ Copy มา:
   ```env
   LINE_CHANNEL_ACCESS_TOKEN=U1234567890abcdefghijklmnopqrstuvwxyz...
   ```
3. **แทนที่** `your_secret_here` ด้วย Secret ที่ Copy มา:
   ```env
   LINE_CHANNEL_SECRET=1234567890abcdefghijklmnopqrstuvwxyz...
   ```
4. **บันทึก** (Ctrl + S)
5. **ปิด** Notepad

**✅ LINE Credentials ตั้งค่าเสร็จแล้ว!**

---

## ขั้นตอนที่ 8: เริ่มต้นระบบ

### 8.1 เปิด Command Prompt หรือ PowerShell

1. เปิด **File Explorer**
2. ไปที่โฟลเดอร์โปรเจกต์
3. **คลิกที่แถบ Address bar**
4. พิมพ์: `cmd` แล้วกด Enter

### 8.2 เริ่มต้น Backend Server

**เปิด Terminal ใหม่ (Terminal 1):**

1. ใน Command Prompt พิมพ์:
   ```cmd
   cd line-webhook
   ```
   กด Enter

2. พิมพ์:
   ```cmd
   npm start
   ```
   กด Enter

3. **รอ** จนเห็นข้อความ:
   ```
   Server is running on port 3000
   ```
   หรือ
   ```
   Listening on port 3000
   ```

**✅ Backend ทำงานแล้ว!** (อย่าปิด Terminal นี้)

### 8.3 เริ่มต้น Frontend Server

**เปิด Terminal ใหม่ (Terminal 2):**

1. กด **Windows + R**
2. พิมพ์: `cmd` แล้วกด Enter
3. พิมพ์:
   ```cmd
   cd "C:\Users\jay_rpn\Documents\Project Big\Project Pos Chat\line chat"
   ```
   กด Enter

4. พิมพ์:
   ```cmd
   npm run dev
   ```
   กด Enter

5. **รอ** จนเห็นข้อความ:
   ```
   Local:   http://localhost:8085
   ```
   หรือ URL อื่นๆ

**✅ Frontend ทำงานแล้ว!** (อย่าปิด Terminal นี้)

### 8.4 เริ่มต้น Cloudflare Tunnel

**เปิด Terminal ใหม่ (Terminal 3):**

1. กด **Windows + R**
2. พิมพ์: `cmd` แล้วกด Enter
3. พิมพ์:
   ```cmd
   cd "C:\Users\jay_rpn\Documents\Project Big\Project Pos Chat\line chat"
   ```
   กด Enter

4. พิมพ์:
   ```cmd
   cloudflared.exe tunnel run line-chat --config cloudflare-tunnel-config.yml
   ```
   กด Enter

5. **รอ** จนเห็นข้อความ:
   ```
   +--------------------------------------------------------------------------------------------+
   |  Your quick Tunnel has been created! Visit it at (it may take some time to be reachable):  |
   |  https://line-chat-backend-12345.trycloudflare.com                                        |
   |  https://line-chat-frontend-12345.trycloudflare.com                                       |
   +--------------------------------------------------------------------------------------------+
   ```

**✅ Tunnel ทำงานแล้ว!** (อย่าปิด Terminal นี้)

---

## ขั้นตอนที่ 9: ตั้งค่า LINE Webhook

### 9.1 เปิด LINE Developers Console

1. เปิดเบราว์เซอร์
2. ไปที่ลิงก์นี้: **https://developers.line.biz/console/**
3. **Login** เข้าระบบ

### 9.2 ไปที่หน้า Webhook Settings

1. **คลิก** ที่ **Provider** และ **Channel** ที่ต้องการ
2. **คลิกแท็บ**: **"Messaging API"** (ด้านบน)
3. **เลื่อนลง** ไปที่ส่วน **"Webhook settings"**

### 9.3 ตั้งค่า Webhook URL

1. **คลิกปุ่ม**: **"Edit"** (ข้าง "Webhook URL")
2. **ใส่ URL**:
   ```
   https://line-chat-backend-12345.trycloudflare.com/webhook/line
   ```
   - **แทนที่** `12345` ด้วยตัวเลขที่ตั้งไว้ใน Dashboard
   - **สำคัญ:** ต้องมี `/webhook/line` ต่อท้าย

3. **คลิกปุ่ม**: **"Update"** หรือ **"Save"**

### 9.4 เปิดใช้งาน Webhook

1. **เปิดสวิตช์**: **"Use webhook"** (ให้เป็นสีเขียว/ON)
2. **ปิดสวิตช์**: **"Enable webhook"** (ถ้ามี) → เปิดเป็น ON

### 9.5 ทดสอบ Webhook

1. **คลิกปุ่ม**: **"Verify"** (ข้าง Webhook URL)
2. **รอ** จนเห็นข้อความ:
   - **"Success"** หรือ **"Verified"** → ✅ สำเร็จ!
   - **"Failed"** → ดู Troubleshooting ด้านล่าง

**✅ LINE Webhook ตั้งค่าเสร็จแล้ว!**

---

## Troubleshooting

### ปัญหา: Tunnel ไม่ทำงาน

**ตรวจสอบ:**
1. Backend Server ทำงานอยู่หรือไม่ (Terminal 1)
2. Frontend Server ทำงานอยู่หรือไม่ (Terminal 2)
3. ไฟล์ `cloudflare-tunnel-config.yml` ถูกต้องหรือไม่
4. Hostname ตรงกับ Dashboard หรือไม่

**แก้ไข:**
- ตรวจสอบว่า Backend และ Frontend รันอยู่
- ตรวจสอบไฟล์ config
- Restart Tunnel (กด Ctrl+C แล้วรันใหม่)

### ปัญหา: Frontend ไม่เชื่อมต่อ Backend

**ตรวจสอบ:**
1. ไฟล์ `.env` (Frontend) มี `VITE_API_URL` หรือไม่
2. URL ใน `.env` ถูกต้องหรือไม่ (ต้องเป็น Backend URL)
3. Restart Frontend หลังจากแก้ไข `.env`

**แก้ไข:**
- แก้ไขไฟล์ `.env`
- Restart Frontend (กด Ctrl+C แล้วรัน `npm run dev` ใหม่)

### ปัญหา: LINE Webhook ไม่ทำงาน

**ตรวจสอบ:**
1. Backend Tunnel ยังทำงานอยู่หรือไม่ (Terminal 3)
2. URL ใน LINE Console ถูกต้องหรือไม่
3. ใช้ HTTPS (ไม่ใช่ HTTP)

**แก้ไข:**
- ตรวจสอบว่า Tunnel ยังทำงานอยู่
- ตรวจสอบ URL ใน LINE Console
- ตรวจสอบว่า Backend Server ทำงานอยู่

### ปัญหา: Login Cloudflare ไม่สำเร็จ

**แก้ไข:**
1. Copy URL จาก Terminal
2. เปิดในเบราว์เซอร์เอง
3. Login และ Authorize

### ปัญหา: ไม่พบ Tunnel ใน Dashboard

**แก้ไข:**
1. ตรวจสอบว่า Login ถูก Account หรือไม่
2. รัน `cloudflared.exe tunnel list` เพื่อดู Tunnel
3. สร้าง Tunnel ใหม่: `cloudflared.exe tunnel create line-chat`

---

## ✅ Checklist สรุป

- [ ] สร้าง Cloudflare Account
- [ ] ดาวน์โหลด cloudflared.exe
- [ ] Login Cloudflare (`cloudflared.exe tunnel login`)
- [ ] สร้าง Tunnel (`cloudflared.exe tunnel create line-chat`)
- [ ] ตั้งค่า Public Hostname (Backend) ใน Dashboard
- [ ] ตั้งค่า Public Hostname (Frontend) ใน Dashboard
- [ ] แก้ไข `cloudflare-tunnel-config.yml`
- [ ] สร้างไฟล์ `.env` (Frontend)
- [ ] สร้างไฟล์ `line-webhook\.env` (Backend)
- [ ] หา LINE Channel Access Token
- [ ] หา LINE Channel Secret
- [ ] ใส่ Token และ Secret ใน `.env`
- [ ] เริ่มต้น Backend Server
- [ ] เริ่มต้น Frontend Server
- [ ] เริ่มต้น Cloudflare Tunnel
- [ ] ตั้งค่า LINE Webhook URL
- [ ] เปิดใช้งาน Webhook
- [ ] ทดสอบ Webhook

---

## 📚 ลิงก์สำคัญ

- **Cloudflare Sign Up**: https://dash.cloudflare.com/sign-up
- **Cloudflare Login**: https://dash.cloudflare.com/login
- **Cloudflare Zero Trust Dashboard**: https://one.dash.cloudflare.com/
- **Cloudflare Tunnel Releases**: https://github.com/cloudflare/cloudflared/releases
- **LINE Developers Console**: https://developers.line.biz/console/
- **LINE Messaging API Docs**: https://developers.line.biz/en/docs/messaging-api/

---

## 🎉 เสร็จแล้ว!

ตอนนี้ระบบพร้อมใช้งาน:
- ✅ Backend URL: `https://line-chat-backend-xxxxx.trycloudflare.com`
- ✅ Frontend URL: `https://line-chat-frontend-xxxxx.trycloudflare.com`
- ✅ URL คงที่ถาวร - ไม่ต้องเปลี่ยน webhook!

**ทดสอบ:**
- เปิด Frontend URL ในเบราว์เซอร์
- ส่งข้อความไปที่ LINE Official Account
- ตรวจสอบว่าได้รับข้อความ

---

<div align="center">
Made with ❤️

⭐ Happy Coding!
</div>

