# 🌐 คู่มือตั้งค่า Cloudflare Tunnel แบบถาวร (URL คงที่)

## 📋 ภาพรวม

ระบบนี้จะทำให้เว็บของคุณมี **URL คงที่ถาวร** ไม่ต้องเปลี่ยน webhook บ่อยๆ เหมาะสำหรับ:
- ✅ LINE Official Account (ไม่ต้องเปลี่ยน webhook URL)
- ✅ แชร์เว็บให้คนอื่นใช้งาน
- ✅ Production หรือใช้งานจริง

---

## 🚀 วิธีตั้งค่า (ทำครั้งเดียว)

### ขั้นตอนที่ 1: สร้าง Cloudflare Account (ฟรี)

1. ไปที่: https://dash.cloudflare.com/sign-up
2. สร้างบัญชีฟรี (ไม่ต้องใส่บัตรเครดิต)
3. Login เข้า Cloudflare Dashboard

---

### ขั้นตอนที่ 2: ดาวน์โหลด Cloudflare Tunnel

1. ไปที่: https://github.com/cloudflare/cloudflared/releases
2. ดาวน์โหลด `cloudflared-windows-amd64.exe`
3. เปลี่ยนชื่อเป็น `cloudflared.exe`
4. วางไว้ในโฟลเดอร์โปรเจกต์ (root)

---

### ขั้นตอนที่ 3: ตั้งค่า Tunnel (อัตโนมัติ)

**วิธีที่ 1: ใช้สคริปต์ (แนะนำ)**
```bash
# ดับเบิลคลิกไฟล์นี้
SETUP_PERMANENT_TUNNEL.bat
```

สคริปต์จะทำ:
1. Login เข้า Cloudflare
2. สร้าง Tunnel ชื่อ "line-chat"
3. สร้างไฟล์ config

**วิธีที่ 2: ทำเอง**
```bash
# 1. Login
.\cloudflared.exe tunnel login

# 2. สร้าง Tunnel
.\cloudflared.exe tunnel create line-chat

# 3. จด Tunnel ID ที่ได้
```

---

### ขั้นตอนที่ 4: ตั้งค่า Public Hostname ใน Cloudflare Dashboard

1. ไปที่: https://one.dash.cloudflare.com/
2. เลือก **Zero Trust** → **Networks** → **Tunnels**
3. คลิก tunnel **"line-chat"**
4. ไปที่แท็บ **"Public Hostnames"**
5. คลิก **"Add a public hostname"**

**ตั้งค่า Backend (port 3000):**
- **Hostname**: `line-chat-backend-xxxxx.trycloudflare.com`
  - เปลี่ยน `xxxxx` เป็นตัวเลขหรือตัวอักษรที่ต้องการ (เช่น `line-chat-backend-12345.trycloudflare.com`)
- **Service**: `http://localhost:3000`
- คลิก **"Add hostname"**

**ตั้งค่า Frontend (port 8085):**
- คลิก **"Add a public hostname"** อีกครั้ง
- **Hostname**: `line-chat-frontend-xxxxx.trycloudflare.com`
  - เปลี่ยน `xxxxx` เป็นตัวเลขหรือตัวอักษรที่ต้องการ (เช่น `line-chat-frontend-12345.trycloudflare.com`)
- **Service**: `http://localhost:8085`
- คลิก **"Add hostname"**

**จด URL ทั้ง 2 ไว้:**
- Backend URL: `https://line-chat-backend-xxxxx.trycloudflare.com`
- Frontend URL: `https://line-chat-frontend-xxxxx.trycloudflare.com`

---

### ขั้นตอนที่ 5: แก้ไขไฟล์ Config

1. เปิดไฟล์ `cloudflare-tunnel-config.yml`
2. แก้ไข hostname ให้ตรงกับที่ตั้งใน Dashboard:

```yaml
tunnel: <TUNNEL_ID>  # ไม่ต้องแก้ (สคริปต์จะใส่ให้อัตโนมัติ)
credentials-file: .cloudflared\<TUNNEL_ID>.json

ingress:
  # Backend (port 3000)
  - hostname: line-chat-backend-12345.trycloudflare.com  # ← แก้ไขตรงนี้
    service: http://localhost:3000
  # Frontend (port 8085)
  - hostname: line-chat-frontend-12345.trycloudflare.com  # ← แก้ไขตรงนี้
    service: http://localhost:8085
  # Catch-all
  - service: http_status:404
```

---

### ขั้นตอนที่ 6: ตั้งค่า Frontend

สร้างไฟล์ `.env` ในโฟลเดอร์ root:

```env
VITE_API_URL=https://line-chat-backend-xxxxx.trycloudflare.com
```

**หมายเหตุ:** เปลี่ยน `xxxxx` ให้ตรงกับ Backend URL ที่ตั้งไว้

---

## 🎯 วิธีใช้งาน (ทุกครั้ง)

### เริ่มต้นระบบ

**วิธีที่ 1: ใช้สคริปต์ (แนะนำ)**
```bash
# ดับเบิลคลิกไฟล์นี้
START_PERMANENT.bat
```

**วิธีที่ 2: รันเอง**
```bash
# Terminal 1: Backend
cd line-webhook
npm start

# Terminal 2: Frontend
npm run dev

# Terminal 3: Tunnel
cloudflared.exe tunnel run line-chat --config cloudflare-tunnel-config.yml
```

---

## 📝 ตั้งค่า LINE Webhook

1. ไปที่: https://developers.line.biz/console/
2. เลือก Provider > Channel > **Messaging API** tab
3. **Webhook settings** > Edit
4. ใส่: `https://line-chat-backend-xxxxx.trycloudflare.com/webhook/line`
5. เปิด **Use webhook** = Enabled
6. กด **Verify**

**✅ URL นี้จะคงที่ถาวร ไม่ต้องเปลี่ยนอีก!**

---

## ✨ ข้อดี

- ✅ **URL คงที่ถาวร** - ไม่ต้องเปลี่ยน webhook บ่อยๆ
- ✅ **ฟรี 100%** - ไม่ต้องเสียเงิน
- ✅ **HTTPS อัตโนมัติ** - ปลอดภัย
- ✅ **ไม่ต้อง Static IP** - ใช้ได้ทุกที่
- ✅ **ไม่ต้อง Forward Port** - ใช้ได้ผ่าน Firewall
- ✅ **ใช้กับ LINE OA ได้** - เหมาะสำหรับ Production

---

## 🆘 Troubleshooting

### ปัญหา: Tunnel ไม่ทำงาน
**แก้ไข:**
- ตรวจสอบว่า Backend และ Frontend รันอยู่
- ตรวจสอบไฟล์ `cloudflare-tunnel-config.yml`
- ตรวจสอบว่า hostname ตรงกับที่ตั้งใน Dashboard

### ปัญหา: Frontend ไม่เชื่อมต่อ Backend
**แก้ไข:**
- ตรวจสอบ `VITE_API_URL` ใน `.env`
- ตรวจสอบว่า Backend Tunnel ยังทำงานอยู่
- Restart Frontend

### ปัญหา: LINE Webhook ไม่ทำงาน
**แก้ไข:**
- ตรวจสอบว่า Backend Tunnel ยังทำงานอยู่
- ตรวจสอบ URL ใน LINE Developers Console
- ตรวจสอบว่าใช้ HTTPS (ไม่ใช่ HTTP)

---

## 📚 อ่านเพิ่มเติม

- [Cloudflare Tunnel Documentation](https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/)
- [Cloudflare Zero Trust Dashboard](https://one.dash.cloudflare.com/)

---

<div align="center">
Made with ❤️

⭐ URL คงที่ถาวร - ไม่ต้องเปลี่ยน webhook บ่อยๆ!
</div>

