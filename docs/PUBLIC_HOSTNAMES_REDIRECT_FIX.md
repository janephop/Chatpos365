# 🔧 แก้ปัญหา: URL /public-hostnames Redirect ไป Overview

## ❓ ปัญหา: URL redirect ไป overview

เมื่อเปลี่ยน URL จาก `/migrate` เป็น `/public-hostnames` แล้วกลับกลายเป็น `/overview` แทน

**หมายความว่า:** URL `/public-hostnames` อาจจะไม่ทำงาน หรือ redirect ไปหน้า overview

---

## ✅ วิธีแก้ไข: ใช้ Routes แทน

### วิธีที่ 1: ใช้ Routes (แนะนำ)

**เปลี่ยน URL เป็น:**

```
https://one.dash.cloudflare.com/f0ba9c6582ac2ca4580daf1c665ecc55/networks/connectors/cloudflare-tunnels/ea4f26bd-8e88-4941-bc4d-2bf4a47e3abd/routes
```

**หรือ:**

```
https://one.dash.cloudflare.com/f0ba9c6582ac2ca4580daf1c665ecc55/networks/connectors/cloudflare-tunnels/ea4f26bd-8e88-4941-bc4d-2bf4a47e3abd
```

แล้วหาแท็บ "Routes" หรือ "Public Hostnames"

---

### วิธีที่ 2: ใช้เมนูใน Dashboard

1. **กลับไปที่หน้า Connectors:**
   ```
   https://one.dash.cloudflare.com/f0ba9c6582ac2ca4580daf1c665ecc55/networks/connectors
   ```

2. **คลิกที่ tunnel "line-chat"** ในตาราง

3. **ดูที่เมนูด้านซ้าย** (ถ้ามี) หรือ **แท็บด้านบน**

4. **หา**: **"Routes"** หรือ **"Public Hostnames"**

---

### วิธีที่ 3: ตั้งค่าผ่าน Routes

**ถ้าเห็นหน้า Routes:**

1. **คลิกปุ่ม**: **"Add a route"** หรือ **"Add route"**

2. **กรอกฟอร์ม:**

   **Hostname:**
   ```
   line-chat-backend-12345.trycloudflare.com
   ```
   - เปลี่ยน `12345` เป็นตัวเลขที่ต้องการ

   **Service:**
   ```
   http://localhost:3000
   ```

3. **คลิก**: **"Add route"** หรือ **"Save"**

4. **ทำซ้ำสำหรับ Frontend** (localhost:8085)

---

## 🔍 วิธีตรวจสอบว่า Public Hostnames อยู่ที่ไหน

### ตรวจสอบใน Dashboard

1. **ไปที่หน้า Tunnel Detail:**
   ```
   https://one.dash.cloudflare.com/f0ba9c6582ac2ca4580daf1c665ecc55/networks/connectors/cloudflare-tunnels/ea4f26bd-8e88-4941-bc4d-2bf4a47e3abd
   ```

2. **มองหาที่ด้านบน** (ใต้ชื่อ tunnel "line-chat")
   - หาแท็บ: **"Routes"**, **"Public Hostnames"**, หรือ **"Hostnames"**

3. **หรือดูที่เมนูด้านซ้าย** (ถ้ามี)

---

## 📋 ขั้นตอนตั้งค่า Public Hostname (ใช้ Routes)

### ขั้นตอนที่ 1: ไปที่หน้า Routes

**ใช้ URL:**
```
https://one.dash.cloudflare.com/f0ba9c6582ac2ca4580daf1c665ecc55/networks/connectors/cloudflare-tunnels/ea4f26bd-8e88-4941-bc4d-2bf4a47e3abd/routes
```

### ขั้นตอนที่ 2: เพิ่ม Route สำหรับ Backend

1. **คลิกปุ่ม**: **"Add a route"** หรือ **"Add route"**

2. **กรอกฟอร์ม:**

   **Hostname:**
   ```
   line-chat-backend-12345.trycloudflare.com
   ```
   - เปลี่ยน `12345` เป็นตัวเลขที่ต้องการ

   **Service:**
   ```
   http://localhost:3000
   ```

3. **คลิก**: **"Add route"** หรือ **"Save"**

### ขั้นตอนที่ 3: เพิ่ม Route สำหรับ Frontend

1. **คลิกปุ่ม**: **"Add a route"** อีกครั้ง

2. **กรอกฟอร์ม:**

   **Hostname:**
   ```
   line-chat-frontend-12345.trycloudflare.com
   ```
   - ใช้ตัวเลขเดียวกันกับ Backend

   **Service:**
   ```
   http://localhost:8085
   ```

3. **คลิก**: **"Add route"** หรือ **"Save"**

---

## 🆘 ถ้ายังไม่เห็น Routes

### ตรวจสอบว่า Tunnel ทำงานอยู่

1. **ดู Status** ในหน้า Detail
2. **ต้องเป็น "DOWN"** (ปกติ - ถ้ายังไม่ได้รัน tunnel)
3. **ไม่เป็นปัญหา** - สามารถตั้งค่า Routes ได้เลย

### ลองวิธีอื่น

1. **รีเฟรชหน้า** (F5)
2. **ลองใช้เบราว์เซอร์อื่น** (Chrome, Edge, Firefox)
3. **เคลียร์ Cache** ของเบราว์เซอร์

---

## ✅ Checklist

- [ ] ใช้ URL `/routes` แทน `/public-hostnames`
- [ ] ไปที่หน้า Routes
- [ ] เพิ่ม Route สำหรับ Backend
- [ ] เพิ่ม Route สำหรับ Frontend
- [ ] แก้ไขไฟล์ config ให้ตรงกับ Routes

---

## 💡 หมายเหตุ

- **Routes** = Public Hostnames (เหมือนกัน)
- บางเวอร์ชันอาจเรียกว่า "Routes" แทน "Public Hostnames"
- ตั้งค่า Routes = ตั้งค่า Public Hostname

---

<div align="center">
✅ **ใช้ Routes แทน Public Hostnames!**

🔗 **URL: `/routes` แทน `/public-hostnames`**
</div>

