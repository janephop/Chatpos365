# 🚀 คู่มือการตั้งค่า LINE Official Account Webhook

## 📋 สิ่งที่ต้องเตรียม

1. **LINE Official Account** พร้อม Channel Access Token และ Channel Secret
2. **ngrok** สำหรับสร้าง public URL
3. **Node.js** (รุ่น 14 ขึ้นไป)

---

## 🛠️ ขั้นตอนการติดตั้ง

### 1️⃣ ติดตั้ง Dependencies

#### Frontend (React)
```bash
npm install
```

#### Backend (Webhook Server)
```bash
cd line-webhook
npm install
```

---

## 🎯 ขั้นตอนการใช้งาน

### 1️⃣ เริ่ม Backend Server

เปิด Terminal แรก:

```bash
cd line-webhook
npm start
```

คุณจะเห็นข้อความ:
```
✅ LINE webhook server is running!
📍 Local: http://localhost:3000
```

---

### 2️⃣ เริ่ม ngrok

เปิด Terminal ที่สอง:

```bash
ngrok http 3000
```

คุณจะเห็น ngrok URL เช่น:
```
Forwarding    https://xxxx-xxx-xxx-xxx.ngrok-free.app -> http://localhost:3000
```

**📋 คัดลอก ngrok URL นี้ (https://xxxx-xxx-xxx-xxx.ngrok-free.app)**

---

### 3️⃣ เริ่ม Frontend

เปิด Terminal ที่สาม:

```bash
npm run dev
```

เปิดเบราว์เซอร์ไปที่ `http://localhost:5173` (หรือ port ที่แสดง)

---

### 4️⃣ ตั้งค่าใน Frontend

1. คลิกไปที่เมนู **Settings** (ไอคอนเฟือง) ในแถบด้านซ้าย
2. เลือก **Channels & Integrations**
3. คลิกที่ **LINE Official Account** card
4. กรอกข้อมูล:
   - **ngrok URL**: วาง URL จาก ngrok (เช่น `https://xxxx-xxx-xxx-xxx.ngrok-free.app`)
   - **Channel Secret**: คัดลอกจาก LINE Developers Console > Basic settings
   - **Channel Access Token**: คัดลอกจาก LINE Developers Console > Messaging API
5. คลิก **บันทึกการตั้งค่า** ✅
6. คลิก **ทดสอบการเชื่อมต่อ** เพื่อตรวจสอบว่า backend พร้อมใช้งาน

---

### 5️⃣ ตั้งค่า Webhook ใน LINE Developers Console

1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. เลือก Provider และ Channel ของคุณ
3. ไปที่แท็บ **Messaging API**
4. หาส่วน **Webhook settings** และคลิก **Edit**
5. ใส่ Webhook URL: `https://xxxx-xxx-xxx-xxx.ngrok-free.app/webhook/line`
   
   > ⚠️ **สำคัญ**: ต้องใส่ `/webhook/line` ต่อท้าย ngrok URL

6. เปิด **Use webhook** เป็น **Enabled**
7. คลิก **Verify** เพื่อทดสอบ (ควรได้ Success)
8. **กรุณาปิด Auto-reply messages** ใน LINE Official Account Manager เพื่อไม่ให้ทับกับ bot

---

## ✅ ทดสอบระบบ

1. เปิด LINE Official Account ของคุณบนมือถือ
2. ส่งข้อความอะไรก็ได้ เช่น "สวัสดี"
3. ดูใน Terminal ที่รัน backend จะมี log แสดงข้อความที่ได้รับ
4. Bot จะตอบกลับ "คุณส่งมา: สวัสดี"

---

## 🔧 Troubleshooting

### ปัญหา: ไม่สามารถเชื่อมต่อ webhook ได้

**วิธีแก้:**
- ตรวจสอบว่า backend server กำลังทำงานอยู่ (`npm start` ใน `line-webhook`)
- ตรวจสอบว่า ngrok กำลังทำงานอยู่
- ตรวจสอบว่า ngrok URL ถูกต้อง (ไม่มี `/` ต่อท้าย)

### ปัญหา: LINE Verify Webhook ไม่ผ่าน

**วิธีแก้:**
- ต้องใส่ `/webhook/line` ต่อท้าย ngrok URL
- ตรวจสอบว่า Channel Secret ถูกต้อง
- คลิก "บันทึกการตั้งค่า" ใน frontend ก่อน

### ปัญหา: ส่งข้อความแล้ว bot ไม่ตอบ

**วิธีแก้:**
- ตรวจสอบว่า Webhook enabled แล้วใน LINE Console
- ตรวจสอบ Channel Access Token ว่าถูกต้อง
- ดู log ใน backend terminal ว่ามีข้อความเข้ามาหรือไม่
- ตรวจสอบว่าปิด Auto-reply แล้วใน LINE OA Manager

---

## 📝 ไฟล์สำคัญ

- `line-webhook/index.js` - Backend webhook server
- `line-webhook/line.js` - LINE SDK configuration
- `src/components/LineSettings.jsx` - Frontend configuration UI
- `src/components/SettingsPage.jsx` - Settings page wrapper

---

## 🔄 หากต้องการเปลี่ยน ngrok URL

เมื่อ restart ngrok URL จะเปลี่ยน ต้องทำดังนี้:

1. คัดลอก ngrok URL ใหม่
2. อัพเดทใน Frontend Settings
3. คลิก "บันทึกการตั้งค่า"
4. อัพเดท Webhook URL ใน LINE Developers Console
5. คลิก Verify อีกครั้ง

---

## 💡 เคล็ดลับ

- ใช้ ngrok free account จะได้ URL ที่เปลี่ยนทุกครั้งที่ restart
- หากต้องการ URL คงที่ ให้ upgrade ngrok เป็น paid plan
- เก็บ Channel Secret และ Access Token ไว้ปลอดภัย อย่าแชร์กับผู้อื่น
- ข้อมูลจะถูกบันทึกใน localStorage และ `.env` file

---

## 🎉 สำเร็จ!

ตอนนี้ระบบ LINE Official Account Webhook พร้อมใช้งานแล้ว! 🚀

หากมีคำถามหรือปัญหา สามารถดู log ใน Terminal ของ backend เพื่อ debug ได้

