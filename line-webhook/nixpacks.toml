[phases.setup]
nixPkgs = ["nodejs-20_x", "python3"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'No build step needed'"]

[start]
cmd = "node index.js"

[variables]
PORT = "3000"

