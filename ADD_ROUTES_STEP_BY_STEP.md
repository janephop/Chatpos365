# 📋 คู่มือเพิ่ม Routes ให้ Tunnel (Step by Step)

## ✅ สถานะปัจจุบัน

จาก Dashboard ที่เห็น:
- ✅ Tunnel "line-chat" **HEALTHY** (ทำงานแล้ว!)
- ✅ Connector ID มีค่า (Service ทำงาน)
- ⚠️ Routes = "--" (ยังไม่ตั้งค่า)

---

## 🚀 ขั้นตอนการเพิ่ม Routes

### ขั้นตอนที่ 1: คลิกที่ชื่อ Tunnel

**ในตาราง Cloudflare Tunnels:**
1. **มองหา tunnel "line-chat"** ในตาราง
2. **คลิกที่ชื่อ "line-chat"** (ข้อความสีน้ำเงินในคอลัมน์ "Tunnel name")
3. **จะไปที่หน้า Tunnel Detail**

---

### ขั้นตอนที่ 2: ไปที่แท็บ Routes

**หลังจากคลิกชื่อ tunnel แล้ว:**

1. **ดูที่ด้านบน** (ใต้ชื่อ tunnel "line-chat")
2. **หาแท็บต่างๆ เช่น:**
   - "Overview" (หรือ "Details")
   - **"Routes"** ← **นี่คือแท็บที่ต้องการ!** ⭐
   - "Public Hostnames" (ถ้ามี)
   - "Settings" (ถ้ามี)

3. **คลิกแท็บ "Routes"**

**หรือใช้ URL โดยตรง:**
```
https://one.dash.cloudflare.com/f0ba9c6582ac2ca4580daf1c665ecc55/networks/connectors/cloudflare-tunnels/ea4f26bd-8e88-4941-bc4d-2bf4a47e3abd/routes
```

---

### ขั้นตอนที่ 3: เพิ่ม Route สำหรับ Backend

**หลังจากเข้าแท็บ Routes แล้ว:**

1. **คลิกปุ่ม**: **"Add a route"** หรือ **"Create a new hostname route"** หรือ **"Add route"**

2. **กรอกฟอร์ม:**

   **Hostname (Required):**
   ```
   line-chat-backend-12345.trycloudflare.com
   ```
   - **สำคัญ:** ต้องมี `.trycloudflare.com` ครบถ้วน
   - เปลี่ยน `12345` เป็นตัวเลขที่ต้องการ (เช่น `12345`, `99999`, `2024`)
   - **ตรวจสอบ:** ต้องไม่มีช่องว่างก่อนหรือหลัง

   **Service หรือ Service URL:**
   ```
   http://localhost:3000
   ```
   - **สำคัญ:** ต้องมี `http://` นำหน้า
   - ใช้ `localhost:3000` สำหรับ Backend

   **Description (ถ้ามี):**
   ```
   Backend API
   ```
   - ไม่บังคับ แต่แนะนำให้กรอก

3. **คลิก**: **"Create"** หรือ **"Save"** หรือ **"Add route"**

4. **รอให้บันทึกสำเร็จ** (ไม่เห็น error)

---

### ขั้นตอนที่ 4: เพิ่ม Route สำหรับ Frontend

**หลังจากเพิ่ม Backend สำเร็จแล้ว:**

1. **กลับไปที่หน้า Routes** (ถ้ายังไม่อยู่)

2. **คลิกปุ่ม**: **"Add a route"** หรือ **"Create a new hostname route"** อีกครั้ง

3. **กรอกฟอร์ม:**

   **Hostname (Required):**
   ```
   line-chat-frontend-12345.trycloudflare.com
   ```
   - ใช้ตัวเลขเดียวกับ Backend หรือต่างกันก็ได้
   - **สำคัญ:** ต้องมี `.trycloudflare.com` ครบถ้วน

   **Service หรือ Service URL:**
   ```
   http://localhost:8085
   ```
   - **สำคัญ:** ต้องมี `http://` นำหน้า
   - ใช้ `localhost:8085` สำหรับ Frontend

   **Description (ถ้ามี):**
   ```
   Frontend Website
   ```
   - ไม่บังคับ แต่แนะนำให้กรอก

4. **คลิก**: **"Create"** หรือ **"Save"**

5. **เสร็จแล้ว!** ✅

---

## ✅ หลังจากเพิ่ม Routes สำเร็จ

### ตรวจสอบ Routes

**ในหน้า Routes ควรเห็น:**
- ✅ `line-chat-backend-12345.trycloudflare.com` → `http://localhost:3000`
- ✅ `line-chat-frontend-12345.trycloudflare.com` → `http://localhost:8085`

**หรือกลับไปที่หน้า Connectors:**
- Routes ควรแสดงเป็น `2` แทน `--`

---

### จด URL ทั้ง 2 ไว้

- ✅ **Backend URL**: `https://line-chat-backend-12345.trycloudflare.com`
- ✅ **Frontend URL**: `https://line-chat-frontend-12345.trycloudflare.com`

**สำคัญ:** 
- URL นี้จะคงที่ถาวร
- ใช้ได้กับ LINE Official Account
- ไม่ต้องเปลี่ยน webhook บ่อยๆ

---

## 🆘 ถ้ามีปัญหา

### ปัญหา: Hostname Route is invalid

**Error:**
```
Hostname Route is invalid: unable to be parsed as a valid hostname
```

**แก้ไข:**
- ตรวจสอบว่า hostname มี `.trycloudflare.com` ครบถ้วน
- ตรวจสอบว่าไม่มีช่องว่างก่อนหรือหลัง hostname
- ดูไฟล์ `FIX_HOSTNAME_ROUTE_ERROR.md` สำหรับรายละเอียด

### ปัญหา: ไม่เห็นแท็บ Routes

**ลอง:**
1. **รีเฟรชหน้า** (F5)
2. **คลิกที่ชื่อ tunnel อีกครั้ง**
3. **ใช้ URL โดยตรง** (ดูด้านบน)

### ปัญหา: ไม่เห็นปุ่ม Add route

**ลอง:**
1. **เลื่อนหน้าลง** - ปุ่มอาจอยู่ด้านล่าง
2. **ดูที่ด้านขวาบน** - อาจมีปุ่ม "Add" หรือ "+"
3. **รีเฟรชหน้า** (F5)

---

## 📋 Checklist

- [ ] คลิกที่ชื่อ tunnel "line-chat"
- [ ] ไปที่แท็บ "Routes"
- [ ] เพิ่ม Route สำหรับ Backend
  - [ ] Hostname: `line-chat-backend-12345.trycloudflare.com`
  - [ ] Service: `http://localhost:3000`
  - [ ] คลิก "Create"
  - [ ] ไม่มี error
- [ ] เพิ่ม Route สำหรับ Frontend
  - [ ] Hostname: `line-chat-frontend-12345.trycloudflare.com`
  - [ ] Service: `http://localhost:8085`
  - [ ] คลิก "Create"
  - [ ] ไม่มี error
- [ ] ตรวจสอบ Routes ในหน้า Connectors (ควรเป็น `2` แทน `--`)
- [ ] จด URL ทั้ง 2 ไว้

---

## 🎉 ขั้นตอนต่อไป

หลังจากเพิ่ม Routes สำเร็จแล้ว:

1. **ตั้งค่า Frontend (.env)**
   - สร้างไฟล์ `.env` ในโฟลเดอร์ root
   - ใส่: `VITE_API_URL=https://line-chat-backend-12345.trycloudflare.com`

2. **เริ่ม Backend และ Frontend**
   - รัน `START_PERMANENT.bat` หรือรันเอง

3. **ตั้งค่า LINE Webhook**
   - ใช้ Backend URL: `https://line-chat-backend-12345.trycloudflare.com/webhook/line`

---

<div align="center">
✅ **คลิกที่ชื่อ "line-chat" แล้วไปที่แท็บ "Routes" เพื่อเพิ่ม Routes!** 🚀
</div>

