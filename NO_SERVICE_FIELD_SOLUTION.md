# 🔧 แก้ปัญหา: ไม่มีช่อง Service URL ในฟอร์ม

## ❌ ปัญหา

เมื่อสร้าง Hostname Route แล้วเห็นแค่:
- Hostname field
- Description field
- **ไม่มีช่อง Service URL!**

---

## ✅ วิธีแก้ไข: สร้าง Route ก่อน แล้วตั้งค่า Service ทีหลัง

### ขั้นตอนที่ 1: สร้าง Route (กรอกแค่ Hostname)

**ในฟอร์ม "Create a new hostname route":**

1. **Hostname (Required):**
   ```
   line-chat-frontend-12345.trycloudflare.com
   ```
   - **สำคัญ:** ต้องมี `.trycloudflare.com` ครบถ้วน

2. **Description:**
   ```
   Frontend Website
   ```
   - หรือ `website hostname` ก็ได้

3. **คลิก "Create"** (แม้จะยังไม่มี Service URL)

---

### ขั้นตอนที่ 2: ตั้งค่า Service หลังจากสร้าง Route

**หลังจากคลิก "Create" แล้ว:**

**วิธีที่ 1: คลิกที่ Hostname ในตาราง**

1. **กลับไปที่หน้า "Hostname routes"**
2. **คลิกที่ชื่อ hostname** `line-chat-frontend-12345.trycloudflare.com` (สีน้ำเงิน)
3. **จะไปที่หน้า Route Detail**
4. **หาช่อง "Service" หรือ "Service URL"** แล้วกรอก:
   ```
   http://localhost:8085
   ```
5. **คลิก "Save"**

---

**วิธีที่ 2: ใช้ปุ่ม Edit**

1. **ในตาราง "Hostname routes"**
2. **มองหาปุ่ม "Edit" หรือไอคอนแก้ไข** (ถ้ามี)
3. **คลิกเพื่อแก้ไข route**
4. **หาช่อง "Service" แล้วกรอก:**
   ```
   http://localhost:8085
   ```
5. **คลิก "Save"**

---

**วิธีที่ 3: ใช้ไฟล์ Config (ถ้า Dashboard ไม่ได้)**

**ถ้าหา Service URL ใน Dashboard ไม่เจอ:**

1. **เปิดไฟล์** `cloudflare-tunnel-config.yml`

2. **ตรวจสอบว่ามี Frontend hostname แล้วหรือยัง:**
   ```yaml
   ingress:
     # Backend (port 3000)
     - hostname: line-chat-backend-12345.trycloudflare.com
       service: http://localhost:3000
     # Frontend (port 8085)
     - hostname: line-chat-frontend-12345.trycloudflare.com
       service: http://localhost:8085
     # Catch-all
     - service: http_status:404
   ```

3. **ถ้ามีแล้ว** → Tunnel จะใช้ config จากไฟล์อัตโนมัติ
4. **ถ้ายังไม่มี** → เพิ่ม Frontend hostname ในไฟล์ config

5. **Restart Service:**
   ```powershell
   Restart-Service Cloudflared
   ```

---

## 🔍 ตรวจสอบว่า Route ทำงาน

**หลังจากตั้งค่า Service แล้ว:**

1. **กลับไปที่หน้า Connectors**
2. **ดูที่คอลัมน์ "Routes"** สำหรับ tunnel "line-chat"
   - ควรแสดงเป็น **`2`** แทน `--` ✅

3. **ทดสอบ URL:**
   - Backend: `https://line-chat-backend-12345.trycloudflare.com`
   - Frontend: `https://line-chat-frontend-12345.trycloudflare.com`

---

## 💡 หมายเหตุ

**สำหรับ Managed Tunnel:**
- Routes ที่สร้างใน Dashboard อาจต้องตั้งค่า Service แยก
- หรือใช้ไฟล์ config ร่วมกัน

**สำหรับ Manual Tunnel:**
- ใช้ไฟล์ config ได้เลย
- Service จะถูกตั้งค่าจากไฟล์ config อัตโนมัติ

---

<div align="center">
✅ **สร้าง Route ก่อน แล้วตั้งค่า Service ทีหลัง!** 🚀

💡 **หรือใช้ไฟล์ config ร่วมกันก็ได้!**
</div>

